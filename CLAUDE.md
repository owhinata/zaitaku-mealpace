# CLAUDE.md — 変えてはいけないこと

このファイルは、実装を担当するエージェント（Claude Code 等）が毎セッション最初に読む。
ここに書かれた制約は、Issue やチャットでの指示より優先する。変更が必要なら
`docs/decisions/` に記録を残してから、このファイルを人が書き換える。

## セッションの入口と出口

- 入口: `docs/status.md` と `docs/plan.md` を読む。担当する Issue の番号を確認する。
- メインのエージェントは管理だけを行い、Issue の作業は subagent が行う。plan mode の出入り（subagent は `ExitPlanMode` を
  実行できない）、Codex のレビュー、コミットはメインが行う（`docs/workflow.md`「メインと subagent」、docs/decisions/0015）。
  subagent として起動された場合は、渡された plan と指示の範囲だけを実行し、git の操作をしない。
- Issue の着手は plan mode から始める。制約領域に触れる plan は、確定前に Codex のレビューを通す
  （`docs/workflow.md`「レビュー（Codex）」、docs/decisions/0004）。
- 出口: `docs/status.md` を更新し、変更点を3行で報告する。
- Issue を close するのは人。特に関門（Milestone 末尾の「判定」Issue）はエージェントが閉じない。

## 作らないもの（企画書 4章）

- **誤嚥の検出・診断。** 外部センサで分かるのは嚥下の有無・タイミング・喉頭の動き・むせまで。
  咽頭残留や不顕性誤嚥は見えない。ラベル、評価指標、変数名、UI 文言のいずれにも
  「誤嚥検出」「aspiration detection」「risk score」に相当する表現を入れない。
- **食形態・姿勢の医学的判断。** 装置は数値を示すだけで、指示を出さない。
- **ベッド・車椅子の自動制御。** 記録と提示までとする。
- **ダッシュボード、設定画面、グラフ UI などの追加機能。** 11月に必要なのは
  検出・LED・記録の3つ。Issue にない機能は作らない。

## データの扱い（企画書 5章・10章）

- 検出器ファームウェア（M2 以降、食卓で使う形態）は生の音声波形を保存も送信もしない。
  特徴量に変換した後に破棄する。
- 記録ファームウェア（`firmware/logger/`）は開発用の計測器で、生の音声を PC に送ってよい。
  PC 側の生波形は下の被験者ごとの規則に従う（docs/decisions/0005）。
- `data/raw/` は .gitignore 済み。生の計測データはいかなる理由でもコミットしない。
  「解析のため一時的に」も不可。
- 被験者 `p1` の生データはローカルの暗号化ディスクにのみ置く。チャット・Issue・
  外部サービスに貼らない（集計値と匿名化した波形の切り出しは可）。
- 被験者 `self` のデータに制限はない。ただし `data/sample/` に置く見本は10秒以内。

## 公開してよい情報の線引き

このリポジトリは public。以下は docs/、log/、Issue、コミットメッセージのすべてに適用する。

- 続柄は「家族」。年齢・性別・氏名・居住地は書かない。
- 疾患は「神経疾患による嚥下障害と片麻痺」まで。個別の病名と服薬名は書かない。
- 写真・動画・音声に本人を含めない。装着写真はマネキンか開発者自身で撮る。
- 被験者 ID は `self` と `p1` のみ。名前や続柄を ID にしない。
- 本人のデータは集計値と匿名化した切り出しのみ。日付は「試用N日目」の相対表記。

## 評価（docs/evaluation.md）

- 学習と評価はセッション単位で分ける。窓単位で分けない。
- `docs/evaluation.md` の定義と合格線を変えない。変える場合は `docs/decisions/` に記録。
- 「85%達成」のような報告には、混同行列と評価に使ったセッション一覧を必ず添える。

## 実装方針（docs/decisions/0001）

- ビルドは CMake がタスクランナーとして arduino-cli を呼ぶ構成（A案）。
- センサ入力とフレーム形式に Arduino の型を漏らさない。Arduino ライブラリの API は
  モジュールの内側だけで呼ぶ。
- 推論は Edge Impulse の **C++ ライブラリ形式**で書き出す（Arduino ライブラリ形式ではない）。
- 解析と評価は PC 側の Python で完結させる。

## 表現

- 「VE の代替」と書かない。「VE の空白を埋める」と書く。
- LED の表示は「目安」であり「判定」ではない。装置の表示・文書の両方でその語を使う。
