# 2026-09-22

## やったこと

- #12 `self` の収集日2を録った（人の作業）。`docs/recording-protocol.md` の順番で7セッション、各 180 秒。
  `20260922-111918_self_quiet` から `20260922-114150_self_cough` まで。
- 集計（下の表）は subagent が `tools/check_session.py` と `events.csv` から出した。特徴量と分類器は
  走らせていない（採否を決める前に見ない決まり）。**採否は人が決める（未決）。**

## #12 収集日2の集計（`events.csv` のマーカー数）

| セッション | cond | 長さ | s | t | c | n | q | o | 取りこぼし（IMU / 音声） |
|---|---|---|---|---|---|---|---|---|---|
| `20260922-111918_self_quiet` | quiet | 179.9 s | 0 | 0 | 0 | 0 | 1 | 0 | 0 / 0.002% |
| `20260922-112234_self_water` | water | 180.0 s | 13 | 0 | 0 | 0 | 0 | 0 | 0 / 0 |
| `20260922-112655_self_talk` | talk | 180.0 s | 0 | 22 | 0 | 0 | 0 | 0 | 0 / 0 |
| `20260922-113026_self_saliva` | saliva | 180.0 s | 12 | 0 | 0 | 0 | 0 | 0 | 0 / 0 |
| `20260922-113435_self_neck` | neck | 180.0 s | 0 | 0 | 0 | 23 | 0 | 0 | 0 / 0.004% |
| `20260922-113810_self_water` | water | 180.0 s | 16 | 0 | 0 | 0 | 0 | 0 | 0 / 0 |
| `20260922-114150_self_cough` | cough | 180.0 s | 2 | 0 | 17 | 0 | 0 | 0 | 0 / 0 |
| 収集日2の合計 | | 21.0 分 | 43 | 22 | 17 | 23 | 1 | 0 | |

収集日1+2の累計（収集日2を採用した場合）: 嚥下 `s` 75（32 + 43）、非嚥下 `t`+`c`+`n` 107
（`t` 42、`c` 27、`n` 38）、`quiet` 6 分（3 + 3）。

- 取りこぼしは7本とも基準内（1%未満）。音声の飛びは `quiet` で1箇所（8 ms、1チャンク）、`neck` で1箇所
  （12 ms、2チャンク）、IMU は7本とも 0。`t_ms` の逆行は IMU・音声とも 0。`record.py` の終了時の XOR 不一致の
  表示は人が見る（エージェントからは見えない）。
- `meta.json` の装置の申告値（`fw` = `logger`、`imu_hz` 104、`audio_hz` 16000）は1本目の `quiet` にだけある。
  残り6本には無い（扱いは docs/decisions/0006 のとおり、`sample_rates` の既定値を使う）。収集日1は7本とも無かった。
- `o` は7本とも 0。打ち遅れ・打ち忘れ・打ち間違いの申告は無い。
- `water` の `s` の間隔が 10 秒を切った箇所は計4つ（1本目 6.9 / 9.4 秒、2本目 9.5 / 9.9 秒）。収集日1の5つより
  減ったが 0 にはなっていない。`[t − 0.5 s, t + 1.5 s]` の区間はどれも重なっていない（最短 6.9 秒）。
  `saliva` の間隔は最短 12.2 秒で、10 秒を切った箇所は無い。
- `quiet`・`talk`・`neck` の計9分で自然な嚥下の `s` は 0 回。`cough` に `s` が2回ある（セッション開始から
  88.6 秒と 124.1 秒。いちばん近い `c` はそれぞれ +4.4 秒と −3.8 秒で、区間は重ならない）。収集日1と同じく、
  打ち忘れが無かったかはデータからは確かめられない。
- 2本目の `water` の最後の `s` は記録の終わり（IMU の最後の `t_ms`）の 0.6 秒前にある。マーカーは記録の範囲の中
  なのでエラーにはならず、嚥下の区間 `[t − 0.5, t + 1.5]` は docs/decisions/0011 のとおり記録の端で切り詰められる。
  収集日1では包絡の最大がマーカーの +1.0 秒付近だったので、この1回は信号の後半が記録に入っていない可能性がある。
- 回数の目安（`water` 10、`talk` 15、`saliva` 5、`neck` 15、`cough` 10 回前後）に対し、`saliva` 12、`neck` 23、
  `cough` 17 と多め。`neck` の `n` の間隔は最短 4.4 秒。目安は合格の条件ではない。
- 収集日1の「次の収集日で気をつけること」のうち、ケーブルの固定と姿勢は人の作業で、log からは確かめられない。
  装着し直しの記録は無い。
- 採否は人が決める（判断材料: `o` 0、取りこぼし 7本とも基準内、`water` の間隔 10 秒未満が4箇所（重なりは無し）、
  `cough` の `s` 2回、2本目の `water` の最後の `s` が記録の端から 0.6 秒、`quiet` の長さ 179.9 秒、XOR 不一致は
  人の画面のみ）。
- 収集日2を採用すれば、「各50回以上」（`s` 50 以上、`t`+`c`+`n` 50 以上かつ各 10 以上、`quiet` 5 分以上）は
  収集日1+2で全部満たす。ただし評価は収集日が3日そろうまで始められない（`train_eval.py` は設計どおり止まる。
  評価側は最後の2収集日で、学習側に1収集日以上が要る）。次に要るのは収集日3。その場合、学習側は収集日1
  （`s` 32 ≥ 20）、評価側は収集日2+3（`s` 43 + 収集日3の分 ≥ 20）になる。

## 確かめていないこと

- `cough` の中の `s` 2回と、`quiet`・`talk`・`neck` で `s` が 0 回だったことが、実際の嚥下の回数と合っているか。
- 収集日2の信号（マーカーの位置の振幅、装着の向き、音の飽和）。採否が決まるまで見ない。
