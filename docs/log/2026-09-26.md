# 2026-09-26

## やったこと

- #24 検出器ファームウェアの段階 2〜3（実機で追いつかせる、60 秒の記録）。段階 1（モジュール、PC のテスト、ビルド）は 9/25 の
  コミット `04a84a9`、段階 2〜3 は `f72fbf6`。実機の操作と `data/raw/` の記録は人、コードとビルドは subagent、記録はこのファイル。
- 実機で最初は窓が 1 つも出なかった（4 スライス目の全窓計算 1.2 秒で毎ホップ捨てる）。`firmware/bench/audio_features.cpp` の再利用版を
  スライスごとの分割計算に変え（PC の答え合わせはビット一致）、IMU スレッドの負担（1 ホップ 293 ms）を切り分け P1〜P6 で測って、
  人の決定で本番の既定を I2C 400 kHz・poll 4 ms・`-Os` にした。1 ホップ 208 ms。
- 装着して 60 秒の記録 `20260926-114039_self_water`（水 5 回）。DETECT / FEAT 206 窓、飛び 0、PC の再採点との差 0。**#24 の完了の条件 4 つを満たした。**

## 分かったこと

- RP2040（FPU なし、`-Os`）で 0020 の式 ＋ int8 の推論 ＋ 送信は 1 ホップ 208 ms（音 188、IMU 12、推論 6、送信 1。壁時計 最大 214 ms）。
  250 ms に収まったので docs/decisions/0001「再検討の条件」には当たらない。余裕は約 40 ms。
- 装置の推論は PC の `score_windows`（同じ EON のコードと `m2_norm.h`）と 206 窓すべてでビット一致（`max |prob − prob_pc|` = 0）。
- IMU の poll スレッドは I2C 100 kHz・2 ms では主スレッドの時間を 138 ms 食う（mbed の I2C は転送中も CPU を使う。plan 第 6 節 (b) の見込みの 3〜4 割を超えた）。
- 送信の遅れ（`t_ms − window_t_ms`）は 1201〜1205 ms、つまり窓の終端から 201〜205 ms。0021 の上限 1000 ms に対して十分小さい。詰めるかは人が決める。
- マーカーの `t_ms` は押した時刻より 0〜0.25 秒早い側に丸まる（装着 120 秒、10 回の実測）。`docs/recording-protocol.md` の前提どおり。

## 次

- 人: 送信の遅れの上限 1000 ms を詰めるか（0021 への追記と `analysis/evaluate_detector.py` の変更は別の作業）。#24 を閉じるか。
- #25 LED（`led` は今は 0）。#27 `meal` の通し記録。実機には本番の detector が入ったまま（logger は書き戻していない）。

## #24 検出器ファームウェア

Issue #24。plan は Codex のレビュー 4 回（初回 CONCERN 4、再レビュー 1 CONCERN 2、再レビュー 2 CONCERN 2、再レビュー 3 で 3 面 LGTM。
人がすべて採用。PLAN-SHA `cf4f308c9b6e0a06`）を通した。人が決めた点 A〜P は plan 第 16 節（Issue #24 のコメントに一覧）。
実施日は段階 1 が 9/25、段階 2〜3 が 9/26。**この節の検出率・誤検出の数字は 1 セッションの参考値で、M2 の合格線の数字は #27 で出す。**

### 条件

- コミット: 段階 1 `04a84a9`（モジュール、PC のテスト、ビルド、`docs/data-schema.md` の META の例を `m2-0020` に修正）、段階 2〜3 `f72fbf6`
  （実機で追いつかせる変更、切り分け、60 秒の記録の確認）。`evaluate_detector.py` の出力のコミット表示は `04a84a9-dirty`（段階 2〜3 のコミット前に実行）。
- 開発環境: arduino-cli 1.5.1、ボードコア arduino:mbed_nano 4.6.0、Arduino_LSM6DSOX、arm-none-eabi-gcc 7-2017q4（ボードコア同梱）、PC は g++ 13.3.0。
- ビルド: `cmake -S . -B build-detector -DSKETCH_NAME=detector -DPORT=… -DPYTHON3=…` → `build` → `upload`。`SKETCH_NAME=detector` のとき CMake が基底フラグ
  `-I firmware/detector/src -DEI_PORTING_MBED=0 -DAF_PROFILE=1 -DEI_CLASSIFIER_TFLITE_ENABLE_CMSIS_NN=0 -DEI_LOG_LEVEL=1` を `build.extra_flags` の先頭に置き、
  `BUILD_FLAGS` はその後ろに足す（plan 第 11 節、決定 H）。本番は `BUILD_FLAGS` 空、計測は `-DDETECTOR_PROFILE=1`。logger は `build`（`SKETCH_NAME=logger`）のまま。
- 本番の既定（`firmware/detector/sensors.h`。人の決定 9/26）: `DETECTOR_I2C_HZ` 400000（`IMU.begin()` の後に `Wire.setClock`）、`DETECTOR_IMU_POLL_MS` 4、
  最適化はボードコアの既定（`-Os`）。`DETECTOR_SEND_FEAT` 1（決定 G）。
- 実機の書き込みと記録は人が行った。subagent はビルドまで。

### ファイル構成と Arduino の閉じ込め（docs/decisions/0001。実物）

`firmware/detector/` 直下（`src/` は #22 の書き出しのまま、編集していない）:

| ファイル | Arduino / mbed の API | 役割 |
|---|---|---|
| `detector.ino` | あり（`Serial`、`millis`、`micros`、`LED_BUILTIN`、`delay`。`DETECTOR_PROFILE` のときは mbed のメモリトレースと RTX のスレッド構造体） | `setup()` / `loop()`。META を 1 回送り、READY のスライスごとに `w1 + 5 ms` まで待って `pipeline_hop` → `frame_send`（DETECT、FEAT）。統計行（`DETECTOR_PROFILE`） |
| `sensors.h/.cpp` | あり（`PDM`、`IMU`、`Wire`、`rtos::Thread`、`ThisThread::sleep_for`、`millis`、`micros`、mbed の critical section。切り分け用に `mbed::Ticker`） | `sensors_begin()`（logger と同じ設定: `IMU.begin()`、`PDM.setBufferSize(512)`、`PDM.begin(1, 16000)`。その後 `Wire.setClock(400000)`）、PDM のコールバック `on_pdm()`（`PDM.available()` → `PDM.read()` → `millis()` → `audio_capture_push_chunk`）、IMU スレッド（AboveNormal、スタック 2048 B、4 ms ごとに poll → `imu_capture_push`）。公開する関数の引数・戻り値は C の型だけ |
| `audio_capture.h/.cpp` | なし | 16 kHz スライス（4000 サンプル）の 2 面（FREE / WRITING / READY / IN_USE）、`t0_ms`、連番、`dropped_chunks`・`pdm_odd_chunks`・`pdm_gaps` |
| `imu_capture.h/.cpp` | なし | IMU の行のリング（192 行）、窓 `[w0, w1)` の取り出し（直前の 1 行も添える）、基準の周期、`imu_window_valid`（0012 の規則の装置での近似） |
| `pipeline.h/.cpp` | なし（`audio_features.h`・`imu_features.h`・`m2_norm.h`・`m2_threshold.h`・`classify.h` を使う） | 1 ホップ: 連番の帳簿 → `audio_reuse_push` → IMU の窓 → `imu_features` → 29 次元 → `m2_normalize` → `classify_run` → `positive`。前段 `pipeline_prepare`（PC のテストが classify 無しで試す）と後段に分けてある |
| `classify.h/.cpp` | なし（Edge Impulse SDK はこの中だけ） | `classify_init()`（`score_windows.cpp` と同じ `check_raw_block` と `static_assert`、`swallow` の添字をラベル名で引く）、`classify_run`、`classify_project_id()`、`classify_deploy_version()` |
| `detector_meta.h/.cpp` | なし | META の JSON（`snprintf`。564 B。`char[768]`） |
| `detector_frames.h` | なし | DETECT（10 B）と FEAT（4 + 4 × 29 B）のペイロードを LE で詰める |
| `frame.h` | リンク → `../logger/frame.h` | `frame_send` |
| `audio_features.h/.cpp`、`fft512.h/.cpp`、`imu_features.h/.cpp` | リンク → `../bench/…` | 特徴量（0020 の式の移植） |
| `m2_norm.h`、`m2_threshold.h`、`src/` | なし | #22 のまま（変えていない） |
| `host/test_detector.cpp`、`host/test_detector.sh` | なし（PC の g++） | Arduino に依存しないモジュールのテスト。**230 件が通る**（`bash firmware/detector/host/test_detector.sh`。SDK 不要、`data/` 不使用） |

- arduino-cli はスケッチ直下のシンボリックリンクの `.h` / `.cpp` を追ってコンパイルする（plan 第 4.8 節。決定 A）。`firmware/logger/` に差分なし（決定 B）。
- `static_assert`（`pipeline.h`）: `AF_PROFILE == 1`、`AF_AUDIO_HZ == 8000`、`AF_N_FEATURES + IMU_N_FEATURES == M2_N_FEATURES`、`AF_IN_HOP_SAMPLES == 4000`。
  `classify.cpp` に `EI_CLASSIFIER_DSP_INPUT_FRAME_SIZE == 29`、ラベル 3、`HAS_DATA_NORMALIZATION == 0` の確認。
- `firmware/detector/README.md` は作っていない（#25 が LED の文言と一緒に作る。0018）。ビルドの手順は `CMakeLists.txt` のコメントと `detector.ino` の冒頭。

### `window_t_ms` の定義と取りこぼし時の挙動

- `window_t_ms` は窓の最初のサンプルの時刻の推定値で、リングにある 4 スライスのうち最も古いスライスの `t0_ms`（決定 F、plan 第 6 節 (d)）。
  `t0_ms = chunk_t_ms − round(k × 1000 ÷ 16000)`（`chunk_t_ms` はチャンクを読んだ直後の `millis()`（0013 の終端側の時刻）、k はそのチャンクのうち面の先頭以降のサンプル数。
  先頭から始まれば −4 ms、後半 32 サンプルからなら −2 ms）。窓の終端は `window_t_ms + 1000`。IMU の窓 `[w0, w1)` も同じ時計（`millis()`）で切る。
  連続するホップの `window_t_ms` の差は 250 ms（60 秒の記録で最大差分 250 ms）。
- 追いつかないとき（決定 E、plan 第 6 節 (c)）: 面の遷移はコールバックが FREE → WRITING → READY、主スレッドが READY → IN_USE → FREE だけ。FREE の面が無い
  （一方が IN_USE、もう一方が READY）ときに届いたチャンクは捨てて `dropped_chunks` を数え、次に始まるスライスの連番を 1 つ飛ばす。`pipeline` は連番の飛びで
  `audio_reuse_init` に戻す（連続でない音声で窓を作らない）。次の窓が出るまで 4 スライス（1 秒）空き、`check_session.py` の `window_t_ms` の飛びとして見える。
- PDM 側の欠落: `n ≠ 128` B のコールバックは `pdm_odd_chunks`、`n == 0` またはコールバックの `millis()` の差が 6 ms 以上なら `pdm_gaps` を数え、WRITING の面の途中までの
  サンプルを捨てて `t0_ms` を次のチャンクで取り直し、飛んだ連番で始める（再レビュー 1・2 の採用）。
- IMU の窓が無効（0012 の規則: 行が 2 未満、行数が 0.9 × 1000 ÷ 周期 未満、窓内の差分が 1.5 × 周期 以上、窓の直前または直後の飛びが窓と交わる）なら DETECT / FEAT を
  出さず `imu_short` を数える（初回・再レビュー 1 の採用）。周期は装置が保持する基準（下の「plan から外れた点」）。
- 起動: `Serial.begin(2000000); while (!Serial) {}` → `pipeline_init()`（表、`classify_init`）→ META を作る → `sensors_begin()` → META を送る。失敗は `LED_BUILTIN` の
  200 ms 点滅で止まる（logger と同じ。0018）。

### ビルドの表示（`arduino-cli compile`。フラッシュの最大 16777216 B、RAM の最大 270336 B）

| ビルド | `BUILD_FLAGS` | スケッチ（フラッシュ） | グローバル変数（静的 RAM） |
|---|---|---|---|
| 本番（段階 1、`04a84a9`） | 空 | 159138 B | 161848 B |
| 計測（段階 1） | `-DDETECTOR_PROFILE=1` | 160970 B | 163656 B |
| **本番（HEAD `f72fbf6`）** | 空 | **159190 B** | **161872 B（59%）** |
| 計測（HEAD） | `-DDETECTOR_PROFILE=1` | 161782 B | 164176 B（60%） |
| 計測 ＋ `-O2`（P6） | `-DDETECTOR_PROFILE=1 -O2`（I2C 400 kHz・poll 4 ms） | 167640 B | （控えなし。`size` の bss は計測ビルドと同じ） |

- HEAD の 2 行は log を書くときに同じソースで再ビルドして控えた（キャッシュあり。9/26 11:39 の `.elf` と同じソース）。警告の数は控えていない。
- plan 第 5.3 節の見込み（静的 142〜160 KB）に対して実測 161872 B。#17 案1（155648 B、ダミーの書き出し）と案2（173660 B）の間。
- `firmware/detector/src/`（1383 ファイル）の RP2040 でのコンパイルは、CMSIS-NN を 0 にした構成で通った（#22 の G1 の確認）。

### 段階 2 の経過（実機で追いつくまで）

実機に書き込んで scratchpad の使い捨てスクリプト `serial_probe.py`（`record.py` の `read_frames` と同じ解析。ヘッダの `t_ms`・`stream_id` と PC の到着時刻だけを持ち、
ペイロードは捨てる。ファイルに書かない。plan 第 13.3 節 m3）で統計行を読んだ。

1. 1 回目: META だけが届き、統計行が 1 本も出なかった。統計行は「成功ホップ 40 回ごと」の設計で、窓が出ないと何も見えない。→ 統計行を時間で出す形に変えた
   （10 秒ごと。ホップが 0 でも出す。累計の `hops` / `hop_false_*` / `slices_taken` / `pdm_chunks_total` / `imu_polls` を足した。**plan 第 10 節から外れる**）。
2. 2 回目: 4 スライス目（最初の窓）の全窓計算が **1177 ms** で、そこから毎ホップ FREE の面が無くチャンクを捨てる（`dropped_chunks` 40%）→ 連番の飛び → リセット → また
   4 スライス目、の繰り返しで窓が 1 つも出ない。IMU の窓は 60〜68 行（取り出しが 2 秒遅れ、リング 192 行（約 1.85 秒）から押し出されていた）。
   → `firmware/bench/audio_features.cpp` の `audio_reuse_push` を、窓が満ちる前のスライスでもそのスライスで決まる末尾の `AF_HOP_FRAMES` フレームだけを計算する形に変えた
   （1 ホップの費用が最初から定常状態と同じになる。**plan の「bench を変えない」・決定 C「無変更で流用」から外れる**。下の「plan から外れた点」）。
   `firmware/bench/host/check_port.py` の出力は変更前後でビット一致。`test_detector` の `reuse first window vs full: max_rel_err=0.000e+00`。
3. 3 回目（= P3）: 音の段が **293 ms** で毎ホップ捨てる（`dropped` 15%）。bench の実測（#21 段階 1: 126 ms、割り込みなし）の 2.3 倍。→ 切り分け P1〜P6（下の表）。
4. 人の決定（9/26）: 本番の既定は P4（I2C 400 kHz、poll 4 ms、`-Os`）。`-O2`（P6）は利得 2% で採らない（PC の `-Os` と条件をそろえる）。
5. P6 の実行で起動後約 11 秒すべての窓が `imu_short`（累計 44）だった。原因の見立て: 基準の周期の推定（plan: 「飛びでない差分（1.5 × 現在の基準 未満）の移動平均」）が、
   poll で 4〜16 ms に量子化された差分の大きい側だけを除いて下に偏り、基準が下がる → 閾値が下がってさらに除く、の自己固定。→ 「直近 512 行の全差分の平均を公称の
   1/1.25〜1.25 倍に収める」に変えた（**再レビュー 2 の採用から外れる**。飛びそのものは窓ごとの規則 (ii)〜(iv) で弾く）。
6. 既定のプロファイル版で机上 60 秒: 1 本目の統計行から窓 37（起動直後の `imu_short` 0）、以後 40 窓 / 10 秒。
7. 本番ビルドで装着して 60 秒の記録（下）。

### 切り分け P1〜P6（`-DDETECTOR_PROFILE=1`、机上、各 40 秒。人が `serial_probe.py --duration 40` で控えた統計行の要点。時間は ms）

| 段 | IMU スレッド | PDM | I2C | poll | 最適化 | `audio_med`（max） | `total_med`（max） | `wall_med` / `wall_max` | `dropped` | 備考 |
|---|---|---|---|---|---|---|---|---|---|---|
| P1 | なし | なし（4 ms のタイマで合成チャンク） | — | — | `-Os` | 128.4〜128.7（128.9） | 128.5〜128.8 | 131〜132 / 133 | 0 | bench の case2 再利用版 126.152 ms と一致（計算は正しい）。`pdm_cb_max` 66 µs |
| P2 | なし | あり | — | — | `-Os` | 154.6〜154.9（155.7） | 154.7〜154.9 | 158〜158.5 / 160 | 0 | PDM の変換（4 ms ごとの割り込み）の負担 ≒ +26 ms（17%）。`pdm_cb_max` 95 µs |
| P3 | あり | あり | 100 kHz（既定） | 2 ms | `-Os` | 292.8 | 293.1 | 337 / 348 | 15% | IMU スレッドの負担 ≒ +138 ms（47%）。毎ホップ捨てる |
| **P4** | あり | あり | **400 kHz** | **4 ms** | `-Os` | 188.0 | 207.4〜207.7（208.4） | 211 / 214 | 0 | **本番の既定（人の決定）。** IMU スレッドの負担 ≒ +33 ms。`imu_feat_med` 12.0（13.8）、`nn_med` 5.6（6.7）、`send_med` 0.7〜0.9（2.8）、`imu_copy_med` 0.17。`imu_rows` 105〜106、`imu_rows_total` 1055 / 10 秒、`imu_polls` 2470 / 10 秒、`pdm_gaps` 0、`pdm_cb_max` 89 µs、`heap_peak` 5644、`stack_main` 1736、`stack_imu` 2048（未計測の疑い）。到着間隔 min 227.9 / median 248.3 / max 269.9、drift −12.2 ms / 40 秒 |
| P5 | あり | あり | 100 kHz | 2 ms | `-O2` | （控えなし） | | | | ビルドはある（`build-detector-p5`）。統計行の控えは材料に無い |
| P6 | あり | あり | 400 kHz | 4 ms | `-O2` | 183.0〜183.4 | 203.0〜203.3（204.7） | 206〜207 / 209 | 0 | P4 より 2% 速い。採らない（人の決定）。起動後約 11 秒は `imu_short`（上の 5.） |

- 読み方: 音の段（`audio`）は P1 128 → P2 155 → P4 188 で、割り込み（PDM の変換 ＋ IMU スレッドの I2C）が主スレッドの時間を食う分。計算そのものは bench と同じ。
  `pdm_cb_max` は 4 ms（4000 µs）に対して 100 µs 未満で、コールバックの複写と面の切り替えは 4 ms に収まる。フィルタ（`Open_PDM_Filter_64`）の時間そのものは未実測
  （P2 − P1 ≒ 26 ms / ホップが割り込み全体の目安）。
- 切り分け用の切り替え（`DETECTOR_PROF_NO_IMU`、`DETECTOR_PROF_NO_PDM`）は `DETECTOR_PROFILE=1` のときだけ通り、本番のビルドでは `#error`。

### 本番の既定（P4）を装着して 120 秒（計測ビルド。静止、水 10 口、少し話す。`s` を 10 回。plan 第 13.1・13.3 節）

- 統計行 12 本すべて: 窓 40 / 10 秒（最初の期間は 37。累計 437 / 440、`not_filled` 3）、`total_med` 207.2〜207.8、`total_max` 208.3〜209.9、`wall_med` 211、
  `wall_max` 213〜214。`audio_med` 187.9〜188.2（max 190.1）、`imu_feat_med` 11.8〜12.0（max 13.8）、`nn_med` 5.2〜5.7（max 6.8）、`send_med` 0.7〜1.4（max 2.8）、
  `imu_copy_med` 0.17（max 1.1）、`wait_max` 5。
- `imu_rows` 105〜106、`imu_rows_total` 1055 / 10 秒、`imu_polls` 2450 / 10 秒。`dropped` 0、`pdm_odd` 0、`pdm_gaps` 0、`pdm_cb_max` 90 µs、`reset` 0、`imu_short` 0、
  `classify_err` 0。`heap_peak` 5644、`stack_main` 1736、`stack_imu` 2048（未計測の疑い）。
- 集計（PC 側）: DETECT 475 / FEAT 475 / META 1、XOR 0。DETECT の到着間隔 min 228.6 / median 248.0 / max 270.9 ms（n 474）。到着時刻 − `t_ms` の drift −1.1 ms / 120 秒。
- **250 ms に収まった**: 1 ホップ 約 208 ms（`total_max` 209.9）、壁時計（スライスが READY になってから送信完了まで）最大 214 ms。余裕は約 40 ms。
  docs/decisions/0001「再検討の条件」（FPU なしで性能が足りない）には当たらない。基板は替えない。
- **RAM の最大**: 静的 161872 B（本番）＋ ヒープ最大 5644 B ＋ 主スレッドのスタック 32768 B（高水位は約 1.7 KB）＋ IMU スレッドのスタック 2048 B（高水位は未計測）
  = 202332 B ≒ **約 200 KB / 264 KB**（arduino-cli の表示は 270336 B）。plan 第 5.3 節の見込み 180〜195 KB よりやや多い。
- **マーカーの `t_ms` と押した時刻の差**（10 回）: `d1` = 押した時刻 − 直前のフレームの到着時刻: min 0.1 / median 154.7 / max 228.2 ms。`d2`（装置の時計での差）:
  min −4.6 / median 162.7 / max 235.2 ms。直前のフレームはすべて FEAT。→ マーカーは押した時刻より **0〜0.25 秒早い側**に丸まる。`docs/recording-protocol.md`
  「押した時刻より最大 0.25 秒早い側に丸まる」の前提と合う。
- 既定のプロファイル版（基準の周期の変更後）で机上 60 秒: 1 本目から窓 37、以後 40 / 10 秒。`total_med` 207.4〜207.6、`total_max` 209.0、`wall_max` 213〜214。
  `dropped` 0、`pdm_gaps` 0、`reset` 0、`imu_short` 0。`stack_main` 1728〜1736、`stack_imu` 2044（Stack Magic Word を飛ばしても塗りつぶしがすべて壊れて見える。未計測のまま）。
  集計: DETECT 235 / FEAT 235、XOR 0、到着間隔 median 248.8（228.5〜270.3）、drift +0.9 ms / 60 秒。

### 60 秒の記録（完了の条件。本番ビルド、装着、`record.py --cond water --duration 60`、水 5 回、`s` 5。plan 第 13.2 節、決定 K・L）

- セッション `data/raw/20260926-114039_self_water`（9/26）。`record.py` の終了時の表示: **DETECT 206 / FEAT 206 / META 1、XOR 不一致 0**。
- `.venv/bin/python tools/check_session.py data/raw/20260926-114039_self_water` の出力（そのまま）:

```
=== data/raw/20260926-114039_self_water ===
[DETECT]
  窓の数: 206
  記録の長さ（DETECT だけの値。目安）: 52.453 秒
  実効レート（装置の送信時刻から。窓の開始の間隔）: 4.00 窓/秒（公称 4 Hz 比 100.0%）
  飛び: 0 箇所 / 失われた窓数(推定): 0 / 取りこぼし率: 0.000%
  window_t_ms の最大差分: 250 ms / window_t_ms の逆行: 0 箇所 / t_ms の逆行: 0 箇所
  送信時刻 − 窓の開始（t_ms − window_t_ms）: 最小 1201 / 中央値 1203 / 最大 1204 ms
[FEAT]
  窓の数: 206
  次元数 N: 29
  記録の長さ（FEAT だけの値。目安）: 52.453 秒
  実効レート（装置の送信時刻から。窓の開始の間隔）: 4.00 窓/秒（公称 4 Hz 比 100.0%）
  飛び: 0 箇所 / 失われた窓数(推定): 0 / 取りこぼし率: 0.000%
  window_t_ms の最大差分: 250 ms / window_t_ms の逆行: 0 箇所 / t_ms の逆行: 0 箇所
  送信時刻 − 窓の開始（t_ms − window_t_ms）: 最小 1201 / 中央値 1203 / 最大 1205 ms
  DETECT との window_t_ms の不一致: 0 窓（DETECT にだけある 0 / FEAT にだけある 0）

[判定] 取りこぼし率 1%未満を「基準内」とする（Issue #7 の作業上の基準。docs/evaluation.md の合格線ではない）
  DETECT:基準内（0.000%）
  FEAT:  基準内（0.000%）
  ※ XOR 不一致数はここでは分からない。tools/record.py の終了時の表示を見ること。
```

- `python analysis/evaluate_detector.py data/raw --sessions 20260926-114039_self_water --scorer analysis/m2_scorer.py` の出力（そのまま。`build-host/score_windows` は
  `bash firmware/detector/host/build.sh` で作ったもの。特徴量・確率の値は含まれない）:

```markdown
# M2 検出器の評価（detect.csv の positive と events.csv の s）

## 実行の条件

- 実行: `python analysis/evaluate_detector.py data/raw --sessions 20260926-114039_self_water --scorer analysis/m2_scorer.py`
- 対象: --sessions で明示したセッション
- コミット: 04a84a9-dirty
- 窓 1.0 秒 / ホップ 0.25 秒。数え方は docs/evaluation.md と docs/decisions/0011、記録の範囲（送信の遅れの上限 1000 ms）は docs/decisions/0021
- `threshold`: 0.949999988（float32 0.949999988。装置の申告値）、`model`: {"deploy_version": 2, "project_id": 1117130, "source": "edge-impulse"}、`feature_set`: 'm2-0020'

## root の検出器のセッション（fw が `detector`。1）

| セッション | cond | 評価 | 理由 |
|---|---|---|---|
| 20260926-114039_self_water | water | 使う |  |

## 評価に使ったセッション（1）

| セッション | cond | 収集日 | 会話 | 嚥下 | 検出 | 誤検出（回） | 非嚥下（分） | 次元数 | FEAT に無い窓 | M2 の数字 |
|---|---|---|---|---|---|---|---|---|---|---|
| 20260926-114039_self_water | water | 20260926 | なし | 5 | 4 | 0 | 0.71 | 29 | 0 | 使わない（cond が meal でない） |

## 合算

合算は出さない（1 セッション。3 セッション未満。M2 の数字ではない）。

## 参考値（docs/decisions/0011「既知の弱点」、#27）

- 常時陽性の場合（全窓を陽性にした場合）: 検出率 1.000（5/5）、誤検出 1 回、誤検出率 1.41 回/分（非嚥下 0.71 分）
- 陽性窓の割合（陽性窓 ÷ 全窓）: 0.078（16/206）
- `positive != (prob >= threshold)` の行数（float32 で比較）: 0
- `c` に紐づく誤検出の塊（塊の最初の窓の中心 − t_c が [-1.0, +5.0] 秒）: `c` 0 回、紐づく塊 0 / 誤検出の塊 0、紐づく塊が 1 つ以上ある `c` の割合 —（0/0）
- 収集日ごと（式は回数の合算。3 セッション未満でも出す）:
  - 20260926: 検出率 0.800（4/5）、誤検出 0 回、誤検出率 0.00 回/分（非嚥下 0.71 分）
- 会話の有無ごと（`o` の note が `conv` のセッションが会話あり）:
  - 会話なし: 検出率 0.800（4/5）、誤検出 0 回、誤検出率 0.00 回/分（非嚥下 0.71 分）

## 再採点（`--scorer analysis/m2_scorer.py`。feat.csv を PC で採点し、閾値 float32 0.949999988 で陽性にした）

| セッション | 窓 | 装置 1 / PC 0 | 装置 0 / PC 1 | max abs(prob − prob_pc) | 装置: 検出 / 誤検出 | PC: 検出 / 誤検出 |
|---|---|---|---|---|---|---|
| 20260926-114039_self_water | 206 | 0 | 0 | 0 | 4/5 / 0 回 | 4/5 / 0 回 |

- 装置の合算: 検出率 0.800（4/5）、誤検出 0 回、誤検出率 0.00 回/分（非嚥下 0.71 分）
- PC の合算: 検出率 0.800（4/5）、誤検出 0 回、誤検出率 0.00 回/分（非嚥下 0.71 分）
- 差のある窓の合計: 装置 1 / PC 0 が 0、装置 0 / PC 1 が 0
```

- `meta.json`（1 フレームで届いた）: `fw` `detector`、`imu_hz` 104、`audio_hz` 16000、`window_ms` 1000、`hop_ms` 250、`threshold` `0.949999988`（`snprintf("%.9g")` は
  実機でも 9 桁で出た）、`feature_set` `m2-0020`、`feature_names` 29 個、`model` `{"source":"edge-impulse","project_id":1117130,"deploy_version":2}`。
  `m2_scorer.check_meta` を通った（`feature_names` は `features_m2.FEATURE_NAMES` と完全一致、`model` は `score_windows` の見出しと一致）。
- **完了の条件（Issue #24 本文）の照合**: (1) ビルドと書き込みが通り、装着 60 秒で DETECT と FEAT が 0.25 秒ごとに届き `window_t_ms` の飛び 0（取りこぼし 0.000% < 1%）
  → 満たす。(2) 同じ 60 秒の `feat.csv` を PC で再採点した陽性と装置の `positive` が一致（装置 1 / PC 0 = 0、装置 0 / PC 1 = 0、`positive != (prob >= threshold)` 0。
  `max |prob − prob_pc|` = 0 で確率もビット一致）→ 満たす。(3) 処理時間と RAM の数字がこの log にある → 満たす。(4) 生の音声を保存も送信もしていないことをコードの
  経路で示す（下の grep）→ 満たす。
- 参考値（1 セッション。M2 の数字ではない）: 検出 4 / 5、誤検出 0 回、非嚥下 0.71 分、陽性窓 16 / 206。#27 の既定の対象（`cond` が `meal`）にも M1 の分割
  （`fw` が `detector` は除外）にも入らない。記録は `data/raw/` に残す（決定 L）。
- 送信の遅れ: `t_ms − window_t_ms` 1201〜1205 ms → 窓の終端（`window_t_ms + 1000`）からの遅れは **201〜205 ms**（1 ホップの処理 208 ms とほぼ同じ。送信は窓の最後の
  スライスが READY になってから 1 ホップの処理の後）。0021 の上限 `MAX_SEND_LAG_MS` 1000 ms に対して十分小さい。**詰めるかは人が決める**（0021 への追記と
  `analysis/evaluate_detector.py` の変更は別の作業。決定 M）。
- 到着間隔（PC 時計）は 120 秒の計測で 228.6〜270.9 ms（median 248.0）で、公称 250 ± 20 ms 程度。装置の `window_t_ms` の間隔はきっかり 250 ms なので、揺れは USB の
  遅れと `millis()` の刻みの側。

### 生の音声が外に出ない経路（plan 第 9 節の grep。9/26、HEAD `f72fbf6` で実行）

1. 送信関数の呼び出し箇所（`src/` を除く。リンク先の bench・logger のファイルも対象）:

```
$ grep -n "frame_send\|Serial\.\|ei_printf" firmware/detector/*.ino firmware/detector/*.h firmware/detector/*.cpp
firmware/detector/detector.ino:162:  Serial.write((const uint8_t*)prof_line, len);   // 数字と見出しだけ（サンプル・特徴量・確率は出さない）
firmware/detector/detector.ino:204:  Serial.begin(2000000);
firmware/detector/detector.ino:215:  frame_send(Serial, FRAME_META, millis(), (const uint8_t*)meta_buf, (uint16_t)n);
firmware/detector/detector.ino:245:    frame_send(Serial, FRAME_DETECT, millis(), p, sizeof p);
firmware/detector/detector.ino:248:    frame_send(Serial, FRAME_FEAT, millis(), f, sizeof f);
firmware/detector/frame.h:19:static void frame_send(Out& out, uint8_t id, uint32_t t_ms, const uint8_t* payload, uint16_t len) {
```

   期待どおり: `frame_send` の呼び出しは `detector.ino` の 3 箇所（META・DETECT・FEAT）だけ（`frame.h:19` は定義）。`Serial.` は `begin`、`frame_send` の引数、
   `DETECTOR_PROFILE` の統計行の `write`（本番のビルドには入らない）だけ。`ei_printf` の呼び出しは無い。`while (!Serial)` は `Serial.` に当たらない（`Serial)`）。

2. `FRAME_AUDIO` / `0x02` の不在（`-r` はシンボリックリンクを追わず 0 件になるので `-R` で実行）:

```
$ grep -Rn "FRAME_AUDIO\|0x02" firmware/detector --include=*.ino --include=*.h --include=*.cpp --exclude-dir=src
firmware/detector/frame.h:8://   FRAME_AUDIO  int16 x N
firmware/detector/frame.h:13:// 検出器ファームウェアは FRAME_AUDIO を送らない（docs/decisions/0005）。形式の決定は docs/decisions/0021。
firmware/detector/frame.h:15:  FRAME_IMU = 0x01, FRAME_AUDIO = 0x02, FRAME_ANALOG = 0x03, FRAME_DETECT = 0x04, FRAME_FEAT = 0x05, FRAME_META = 0x7F
```

   期待どおり: `frame.h`（リンク先 `firmware/logger/frame.h`）の enum の定義行とコメントだけ。検出器のコードに `FRAME_AUDIO` は無い。

3. 各フレームのペイロード: META は固定の JSON（上）。DETECT は `window_t_ms` u32 ＋ `prob` float32 ＋ `positive` u8 ＋ `led` u8（10 B）。FEAT は `window_t_ms` u32 ＋
   正規化前の特徴量 float32 × 29（IMU 14 ＋ 40 フレームの平均の MFCC 13・重心・ゼロ交差率。波形の標本は含まない。0021）。

4. 16 kHz のスライスと 8 kHz のリングの参照箇所:

```
$ grep -n "slice\|ring" firmware/detector/audio_capture.cpp firmware/detector/pipeline.cpp firmware/detector/detector.ino
firmware/detector/pipeline.cpp:5:#include <string.h>
firmware/detector/pipeline.cpp:8:static uint32_t s_slice_t0[PIPELINE_N_SLICES];   // 直近 4 スライスの先頭の時刻（ms）。添字 0 が最も古い
firmware/detector/pipeline.cpp:9:static uint32_t s_slices_in_run;                 // リセット後に受け取ったスライスの数（4 で飽和）
firmware/detector/pipeline.cpp:25:  s_slices_in_run = 0;
firmware/detector/pipeline.cpp:28:  memset(s_slice_t0, 0, sizeof(s_slice_t0));
firmware/detector/pipeline.cpp:40:  // 次のスライスを足したとき最も古くなるのは s_slice_t0[1]（3 スライス受け取り済みなら）。それ以外は公称で t0 + 250
firmware/detector/pipeline.cpp:41:  if (s_slices_in_run >= PIPELINE_N_SLICES - 1) return s_slice_t0[1] + PIPELINE_WINDOW_MS;
firmware/detector/pipeline.cpp:45:bool pipeline_prepare(const int16_t* slice16k, uint32_t t0_ms, uint32_t seq, const ImuWindowSource* imu, HopResult* r) {
firmware/detector/pipeline.cpp:57:    s_slices_in_run = 0;
firmware/detector/pipeline.cpp:63:  for (uint32_t i = 1; i < PIPELINE_N_SLICES; i++) s_slice_t0[i - 1] = s_slice_t0[i];
firmware/detector/pipeline.cpp:64:  s_slice_t0[PIPELINE_N_SLICES - 1] = t0_ms;
firmware/detector/pipeline.cpp:65:  if (s_slices_in_run < PIPELINE_N_SLICES) s_slices_in_run++;
firmware/detector/pipeline.cpp:70:  bool filled = audio_reuse_push(&s_st, slice16k, r->features + IMU_N_FEATURES);   // 16 kHz → 間引いて 8 kHz リングへ
firmware/detector/pipeline.cpp:80:  uint32_t w0 = s_slice_t0[0], w1 = w0 + PIPELINE_WINDOW_MS;
firmware/detector/pipeline.cpp:108:bool pipeline_hop(const int16_t* slice16k, uint32_t t0_ms, uint32_t seq, const ImuWindowSource* imu, HopResult* r) {
firmware/detector/pipeline.cpp:109:  if (!pipeline_prepare(slice16k, t0_ms, seq, imu, r)) return false;
firmware/detector/detector.ino:124://   3 IMU 不足/4 推論エラー）imu_rows_last classify_err_last slices_taken slices_ready_max dropped_chunks pdm_odd_chunks pdm_gaps
firmware/detector/detector.ino:126:// 読み方: pdm_chunks_total が 10 秒で約 2500 増え pdm_bytes_last が 128 なら PDM は届いている。slices_taken が 40 増えなければ面が満ちていない。
firmware/detector/detector.ino:149:               " slices_taken=%lu slices_ready_max=%lu dropped_chunks=%lu pdm_odd_chunks=%lu pdm_gaps=%lu pdm_chunks_total=%lu pdm_bytes_last=%lu"
firmware/detector/detector.ino:156:               (unsigned long)ss.slices_taken, (unsigned long)ss.slices_ready_max, (unsigned long)ss.dropped_chunks,
firmware/detector/detector.ino:226:  const int16_t* slice; uint32_t t0, seq, ready_ms;
firmware/detector/detector.ino:227:  if (!sensors_take_slice(&slice, &t0, &seq, &ready_ms)) return;       // READY の面が無ければ次の loop へ
firmware/detector/detector.ino:238:  bool ok = pipeline_hop(slice, t0, seq, sensors_imu_source(), &r);
firmware/detector/detector.ino:239:  sensors_release_slice();                                             // 16 kHz の面を空ける（次の書き込みで上書きされる）
firmware/detector/audio_capture.cpp:3:#include <string.h>
firmware/detector/audio_capture.cpp:96:      s_stats.slices_started++;
firmware/detector/audio_capture.cpp:113:bool audio_capture_take(const int16_t** slice, uint32_t* t0_ms, uint32_t* seq, uint32_t* ready_t_ms) {
firmware/detector/audio_capture.cpp:124:  s_stats.slices_taken++;
firmware/detector/audio_capture.cpp:128:  *slice = face.buf;
```

   16 kHz のスライス（`slice16k`）は `pipeline.cpp:70` で `audio_reuse_push` に渡るだけ。8 kHz のリングは `AudioReuseState`（`audio_features.cpp`）の中にあり、
   `pipeline.cpp` からは触らない。どちらも `frame_send` に渡らない（1. の 3 箇所の引数は `meta_buf`、`p`（DETECT 10 B）、`f`（FEAT 120 B））。

5. `DETECTOR_PROFILE` の統計行は数字と見出しだけ（`detector.ino` の `prof_emit` のコメントに明記。サンプル・特徴量・確率を出すコードは無い）。本番のビルドには入らない。
6. 装置が出す文字列は META、統計行（計測ビルドのみ）、SDK のエラー文（`EI_LOG_LEVEL=1`。値ではなくメッセージ）だけ。

### plan から外れた点

1. **`firmware/bench/audio_features.cpp` を変えた**（plan 第 3 節「`firmware/bench/` は変えない」、決定 C「`audio_reuse_push` を無変更で流用」から外れる）。
   再利用版が最初の窓で全フレーム（40 フレーム、FFT 40 回）をまとめて計算する設計だったため、4 スライス目に 1177 ms 掛かって装置が追いつけなかった。
   窓が満ちる前のスライスでも末尾の 10 フレームだけを計算する形に変え（M2 はフレームごとのプリエンファシスなので新しいスライスのサンプルだけで決まる。
   M1 の経路も窓が満ちた時点のリングが同じ値になる）、`check_port.py` の出力は変更前後でビット一致、`test_detector` の全窓版との差 0。式（0020）は変えていない。
2. **IMU の基準の周期の推定**（再レビュー 2 の採用「飛びでない差分（1.5 × 現在の基準 未満）の移動平均（512 行）」から外れる）。装置の `t_ms` は poll（4 ms）で量子化されて
   差分が 4〜16 ms に散り、大きい側だけを除くと基準が下に偏って自己固定する（P6 で起動後約 11 秒すべての窓が無効）。「直近 512 行の全差分の平均（= 経過時間 ÷ 行数）を
   公称 9.6 ms の 1/1.25〜1.25 倍に収める」に変えた。飛び G ms の影響は G ÷ 511 ms に留まり、飛びそのものは窓ごとの規則 (ii)〜(iv) で弾く。`features.py` の `valid` との
   突き合わせのテストはそのまま通る。
3. **I2C のクロックと poll 間隔の既定**（決定 D「I2C はボードコアの既定、2 ms poll」から外れる。決定 P の 1・2 段目を既定にした）。`sensors.h` の `DETECTOR_I2C_HZ`
   400000 と `DETECTOR_IMU_POLL_MS` 4 で切り替えられる（0 で既定のまま）。logger の記録（I2C は既定）と IMU の読み出しの条件が違う（読む値は同じレジスタ。影響は見ていない）。
4. **`-O2` は計測したが採らない**（決定 P の 3 段目。P4 で 250 ms に収まったので要らず、利得 2% で PC の `-Os` と条件をそろえる。人の決定）。DRDY 割り込み（4 段目）は未実装。
5. **統計行の設計変更**（plan 第 10 節「40 ホップごと」から外れる）: 10 秒ごとに時間で出す（ホップが 0 でも出る）。項目に累計の `hops`・`windows_total`・`hop_false_*`・
   `last_reason`・`slices_taken`・`slices_ready_max`・`pdm_chunks_total`・`pdm_bytes_last`・`imu_rows_total`・`imu_polls`、期間の `wait_max`、ビルドの切り替え
   （`imu_thread`、`pdm`、`i2c_hz`、`poll_ms`）を足した。
6. **切り分け用の切り替え** `DETECTOR_PROF_NO_IMU`（IMU スレッドを起動しない）と `DETECTOR_PROF_NO_PDM`（PDM の代わりに 4 ms のタイマで固定の合成チャンク）を足した
   （plan に無い。`DETECTOR_PROFILE=1` のときだけ通り、本番では `#error`）。
7. 小さい差: `sensors_take_slice` に `ready_t_ms`（壁時計の計測用）を足した。`pipeline_hop` を前段 `pipeline_prepare` と後段に分けた（PC のテストが classify 無しで前段を試す）。
   IMU の最後の行の待ちは plan どおり `w1 + 5 ms`（`wait_max` 5）。

### 確かめていないこと

- IMU スレッドのスタックの高水位（走査が 2048 → Stack Magic Word を飛ばしても 2044 を返す。塗りつぶしの時期（`rtos::Thread` の生成がスタックを先に触る）の問題と推定。
  スタック 2048 B が足りているかは、動作している事実（120 秒 ＋ 60 秒で `imu_rows` 105〜106、落ちない）からの推定）。
- DRDY 割り込み（`INT_IMU`、GPIO 21 の配線）。未実装・未確認（要らなかった）。
- PDM のフィルタ（`Open_PDM_Filter_64`）の時間そのもの（P2 − P1 の差 約 26 ms / ホップが割り込み全体の目安）。
- 装置で計算した特徴量そのものの正しさは、bench の合成信号の答え合わせ（#21 段階 1: 最大相対誤差 6.4e-7）と、今回の分割計算の変更でのビット一致に依る。
  実信号での PC との突き合わせは、検出器が波形を送らない設計なのでできない（設計どおり）。装置と PC の一致（`--scorer`）は「同じ `feat.csv` を同じモデルに入れる」の一致で、
  特徴量の計算の一致ではない。
- #22 の EI との差あり 176 窓が装置でも同じ側に出るか（60 秒の記録は収集日4 の窓ではないので見られない。装置と PC の `score_windows` は同じ EON のコードでビット一致）。
- 60 秒の記録の窓の数 206（52.5 秒）が plan の見込み 236〜238 より少ない理由（`record.py` の開始から最初の窓までの時間、`while (!Serial)` の待ちと `pipeline_init` の
  表づくり、装置の起動と記録の開始のずれのどれか。材料に無い。取りこぼしではない: 飛び 0、`window_t_ms` の差分は全部 250 ms）。
- 長時間の安定性（記録ファームウェアは収集日3 で約 22 分後に止まった例がある）。#27 の `meal`（最長 30 分）で見る。
- 60 秒の記録の IMU の取りこぼしは `feat.csv` からは分からない（計測ビルドの `imu_rows` 105〜106、`imu_short` 0 からの推定）。
- `while (!Serial) {}` により、ホストがポートを開くまで装置は動かない（logger と同じ）。介助者向けの単独動作は #25 以降の論点。
- USB を挿し直さずに `record.py` を始めると META が届かない（0006）。今回は挿し直して META 1 が届いた。
- 実機には本番の detector（`f72fbf6`、`BUILD_FLAGS` 空）が入ったまま。logger は書き戻していない。
