# 2026-09-23

## やったこと

- #12 `self` の収集日3を録った（人の作業）。`docs/recording-protocol.md` の順番で7セッション、各 180 秒。
  `20260923-220547_self_quiet` から `20260923-223530_self_cough` まで。途中で記録ファームウェアがフレームを
  送らなくなり、`cough` の前に基板を挿し直した（下の「記録ファームウェアの停止」）。
- 集計（下の表）は subagent が `tools/check_session.py` と `events.csv` から出した。特徴量と分類器は
  走らせていない（採否を決める前に見ない決まり）。**採否は人が決める（未決）。**
- 人の話では、収集の最終日は明日（9/24、収集日4）。

## #12 収集日3の集計（`events.csv` のマーカー数）

| セッション | cond | 長さ | s | t | c | n | q | b | o | 取りこぼし（IMU / 音声） |
|---|---|---|---|---|---|---|---|---|---|---|
| `20260923-220547_self_quiet` | quiet | 179.9 s | 0 | 0 | 0 | 0 | 1 | 0 | 0 | 0.005% / 0.004% |
| `20260923-220928_self_water` | water | 179.8 s | 10 | 0 | 0 | 0 | 0 | 0 | 0 | 0 / 0 |
| `20260923-221337_self_talk` | talk | 180.1 s | 1 | 22 | 0 | 0 | 0 | 0 | 0 | 0 / 0 |
| `20260923-221714_self_saliva` | saliva | 179.9 s | 12 | 0 | 0 | 0 | 0 | 0 | 0 | 0 / 0 |
| `20260923-222039_self_neck` | neck | 179.9 s | 0 | 0 | 0 | 19 | 0 | 0 | 0 | 0 / 0 |
| `20260923-222420_self_water` | water | 180.0 s | 12 | 0 | 0 | 0 | 0 | 0 | 0 | 0 / 0.004% |
| `20260923-223530_self_cough` | cough | 180.0 s | 0 | 0 | 11 | 0 | 0 | 0 | 0 | 0 / 0 |
| 収集日3の合計 | | 21.0 分 | 35 | 22 | 11 | 19 | 1 | 0 | 0 | |
| `20260923-223501_self_cough`（空。合計に入れない） | cough | 0 | 0 | 0 | 2 | 0 | 0 | 0 | 0 | 行なし（検査は「データ行が2行未満」で止まる） |

収集日1+2+3の累計（収集日2と3を採用した場合）: 嚥下 `s` 110（32 + 43 + 35）、非嚥下 `t`+`c`+`n` 159
（`t` 64、`c` 38、`n` 57）、`quiet` 9 分（3 + 3 + 3）。

- 取りこぼしは7本とも基準内（1%未満）。IMU の飛びは `quiet` で1箇所（18 ms、1行）、音声の飛びは `quiet` と
  2本目の `water` で1箇所ずつ（12 ms、2チャンク）。`t_ms` の逆行は IMU・音声・`events.csv` とも 0。
  範囲の外のマーカーは 0。`record.py` の終了時の XOR 不一致の表示は人が見る（エージェントからは見えない）。
- `meta.json` の装置の申告値（`fw` = `logger`、`imu_hz` 104、`audio_hz` 16000）は1本目の `quiet` と、挿し直した後の
  `cough`（`223530`）にだけある。残り5本と空の `223501` には無い（扱いは docs/decisions/0006 のとおり、
  `sample_rates` の既定値を使う）。2本とも記録の最初の `t_ms` が小さく（`quiet` 41671、`cough` 7504）、基板が
  起動して間もなく録り始めた本にだけ申告値が入っている（0006 の、リセットせずに始めると META が届かないという説明と合う）。
- `o` は7本とも 0。打ち遅れ・打ち忘れ・打ち間違いの申告は無い。
- `water` の `s` の間隔は最短 14.4 秒（1本目）と 11.2 秒（2本目）で、10 秒を切った箇所は無い（収集日1は5つ、
  収集日2は4つ）。`[t − 0.5 s, t + 1.5 s]` の区間は重ならない。`saliva` の間隔は最短 11.1 秒。
- `quiet`・`talk`・`neck` の計9分で自然な嚥下の `s` は1回（`talk`、セッション開始から 165.3 秒。いちばん近い `t` は
  +3.7 秒で、区間は重ならない）。`cough` に `s` は無い。収集日1・2と同じく、打ち忘れが無かったかはデータからは
  確かめられない。
- 記録の端に近いマーカー: `neck` の最後の `n` が記録の終わりの 0.8 秒前。`n` は評価の式に入らないので影響は無い。
  `s` はどのセッションでも記録の端から 2 秒以上離れている（`water` の最後の `s` は終わりの 6.3 秒前と 9.4 秒前）。
  収集日2のような、嚥下の区間が記録の端で切り詰められる `s` は無い。
- 回数の目安（`water` 10、`talk` 15、`saliva` 5、`neck` 15、`cough` 10 回前後）に対し、`talk` 22、`saliva` 12、
  `neck` 19 と多め。`neck` の `n` の間隔は最短 8.1 秒、`cough` の `c` は最短 11.1 秒。目安は合格の条件ではない。
- 記録は夜（22:05〜22:38）。収集日1・2は午前だった。姿勢・ケーブルの固定は人の作業で、log からは確かめられない。

## 記録ファームウェアの停止（1回きり。様子を見る）

- 22:32 ごろ、`cough` を録ろうとしたところで、記録ファームウェア（`firmware/logger/`）がフレームを送らなくなった。
  USB の列挙は残ったまま、`/dev/ttyACM0` から 3 秒間 0 バイト。そのとき作った `20260923-223211_self_cough` は
  フレーム 0（マーカーの `t_ms` が 0 ms のまま）で、そのフォルダは今は無い。
- PC が最後に受け取ったフレームは2本目の `water` の最後（IMU の `t_ms` = 1334831、基板が約 22 分続けて動いた時点）。
  `record.py` はセッションの間ポートを読まないので、基板が止まったのがこの直後か、22:32 の直前かは分からない。
- `20260923-223501_self_cough` も空。`imu.csv`・`audio_chunks.csv` はヘッダーだけ（0 行）、`audio.wav` は 44 バイトの
  ヘッダーだけ（16 kHz・モノラル・16 bit、データ 0 バイト）、`events.csv` は `c` が2つでどちらも `t_ms` = 0、
  `meta.json` に装置の申告値は無い。`223211` と同じ症状。
- 人が基板を挿し直して回復した。次の `20260923-223530_self_cough` は最初の `t_ms` が 7504 で、基板が起動して約 7.5 秒で
  録り始めたと読める。挿し直しは `223501` と `223530` の間だったことになる。`223530` を収集日3の `cough` とみなして
  上の表に入れた。
- 人の判断で Issue は立てない。1回きりの出来事として、収集日4で同じことが起きないかを見る（起きたら、止まるまでの
  時間と、直前の `t_ms` を控える）。
- 挿し直しのときに基板を首から外したかは記録が無い（外していれば、`docs/recording-protocol.md` のとおり `cough` から
  付け直したことを log に書く）。人に確かめる。

## 採否の判断材料（人が決める。未決）

- `o` 0、取りこぼし 7本とも基準内、`water` の間隔 10 秒未満は 0、`talk` の `s` 1回、`neck` の最後の `n` が記録の端から
  0.8 秒、`cough` は挿し直した後の1本、XOR 不一致は人の画面のみ。
- `20260923-223501_self_cough`（空）の扱い。`docs/recording-protocol.md` の「中断・起動失敗でできたもの」に当たる形。
  `data/raw/` に残すと、`analysis/split.py` は名前の形式と `meta.json` の `subject` が合うので `self` のセッションとして
  数え、`--eval-min` がずれる。評価側に入れば、記録の範囲（docs/decisions/0011）が出せずに止まる。消すかどうかは人が決める。
- 収集日2の採否もまだ未決（docs/log/2026-09-22.md）。

## 評価を始められるか（分割の決まりに照らして）

- 収集日2と3を採用し、空の `223501` を除けば、「各50回以上」は満たし（`s` 110、`t`+`c`+`n` 159 で `t`・`c`・`n` とも
  10 以上、`quiet` 9 分）、学習側に1収集日以上・評価側に2収集日もそろう。その場合、学習側は収集日1（`s` 32 ≥ 20）、
  評価側は収集日2+3の14本（`s` 43 + 35 = 78 ≥ 20、`--eval-min 14`）。決まりの上では、この時点で評価を始められる。
- ただし人は明日（9/24）を最終の収集日にするつもりでいる。収集日4を録って採用すれば、評価側は収集日3+4、学習側は
  収集日1+2（`s` 75）になる。#13 で評価側の数字を一度見た後は評価側を動かさない決まりなので、収集日4を録るなら、
  #13 は収集日4の採否を決めた後に回す。どちらにするかは人が決める。
- #13 の前に、`analysis/split.py data/raw --eval-min <最後の2収集日のセッション数>` の `eval` が最後の2収集日の
  セッション名の一覧と一致することを目で照合する（`docs/recording-protocol.md`）。

## 確かめていないこと

- `talk` の中の `s` 1回と、`quiet`・`neck`・`cough` で `s` が 0 回だったことが、実際の嚥下の回数と合っているか。
- 記録ファームウェアが止まった原因と時刻。
- 収集日3の信号（マーカーの位置の振幅、装着の向き、音の飽和）。採否が決まるまで見ない。
