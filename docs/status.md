# 現在地

更新: 2026-09-26

## 段階

M2 検出器（9/28〜10/11）。M1 ロギング・分岐点の関門は 9/24 に通過（docs/decisions/0017、`gate-M1` = `b70a0cb`）。
M0 準備の関門は 9/20 に通過（docs/decisions/0010、`gate-M0`）。

## 直近の状態

- リポジトリ初期化。文書・骨格・記録スクリプトを配置。
- GitHub に Milestone M0〜M6 とラベル3つを作成。
- M0 の Issue（#1〜#6）と M1 の Issue（#7〜#15）を起票。
- Codex のレビュールールと plan レビューのゲートを配置（docs/workflow.md、docs/decisions/0004）。
  承認は plan ファイルのハッシュと Codex 出力の PLAN-SHA 行に結びつく。hook は判定できないとき block する。
  hook は単体で動作確認済み（承認→通過、plan を1文字変えると block）。
- #1: 開発環境を導入し、`firmware/logger` のビルドが無修正で通った（arduino:mbed_nano 4.6.0、docs/log/2026-09-19.md）。
  Python 依存は `.venv` に入れ、`-DPYTHON3=` で渡す。
- #2: 実機への書き込みと10秒の記録が通った。META 1 / IMU 1045 / AUDIO 2475 フレーム、XOR 不一致 0、
  実効 105.5 Hz / 16006.5 Hz（docs/log/2026-09-19.md）。`record` ターゲットの引数の不具合を直し、
  `tools/record.py` に `--duration` とフレーム数の表示を足した。`docs/data-schema.md` の記載漏れ2点は
  docs/decisions/0006 に記録。音声チャンクの `t_ms` の食い違いは #15 に切り出した。
- Codex を実際に呼ぶ plan レビューを #2 で初めて通した（3回、計約41分、CONCERN 10件、BLOCKING 0）。
- #3: 生の音声波形の線引きを docs/decisions/0005 に記録し、AGENTS.md・docs/workflow.md・codex-review skill を直した。
  CLAUDE.md の該当行も差し替え済み。
- #4: Edge Impulse のプロジェクト `zaitaku-mealpace` を作成（Private、One label per data item、
  Target は Raspberry Pi RP2040 / RAM 264 KB / ROM 16 MB、docs/log/2026-09-20.md）。データは未投入。
- #6: 関門のレビュー（Codex、base は初期コミット）を1回回した。公開情報の線引きへの指摘はなし。
  指摘は1件: `tools/record.py` の `--out` と `.gitignore` の `!data/sample/**` により、p1 の生データを
  コミットできる場所へ記録できる。対応として `--out` を廃止し、出力先を `data/raw/` に固定した
  （docs/decisions/0007）。リポジトリ外の cwd から3秒の記録で確認済み。`git add -f` と手での移動は塞いでいない。
  再レビュー1回目の指摘（`cond` に `../` を入れると `data/raw/` の外に記録できる）には、`cond` を英数字と
  ハイフンに限り、`Session` が出力先の直下にしか作らない形で対応した。
  再レビュー2回目（最後）の指摘は1件: `data/raw` をシンボリックリンクに差し替えると直下の検査を抜ける。
  今の `data/raw` は実ディレクトリで、違反は起きていない。人の判断で CONCERN とし、運用で注意する
  （docs/decisions/0007「塞がっていない経路」）。関門のレビューに BLOCKING は残っていない。
- #6: plan の再レビューは前回の Codex スレッドを resume する（docs/decisions/0008）。初回と関門は fresh。
  再レビューの回数の上限はなくした。止める条件は BLOCKING の有無と人の採否（docs/decisions/0009）。
- #7: 喉元に装着して60秒記録した（`self` / `quiet`）。IMU 6321 / AUDIO 14976 フレーム、XOR 不一致 0、
  `t_ms` の飛びは IMU・音声とも 0 箇所（取りこぼしの推定 0.000%）、実効 105.51 Hz / 15999.7 Hz
  （docs/log/2026-09-20.md）。ファームウェアは直していない。数えるために `tools/check_session.py` を足した
  （`imu.csv` と `audio_chunks.csv` の `t_ms` の飛びから取りこぼしを推定する。以降の記録でも使う）。
- #8: `self` の記録手順を `docs/recording-protocol.md` に固定した。1セッション 180 秒、1収集日 = 7セッション
  （`water` ×2、`saliva`、`talk`、`cough`、`neck`、`quiet`）。評価に回すのは最後の2収集日のセッション全部で、
  日数は先に固定しない。非嚥下の「50回」は `t`+`c`+`n` の合計50以上・各10以上・`quiet` 合計5分以上。
  手順を決める前のセッション（#2・#6・#7 の確認用）は M1 の学習・評価に使わない。
  plan レビューは1回（BLOCKING 0、CONCERN 4、採否は #8 のコメント）。
- #10: `analysis/evaluate.py` と `analysis/split.py` に、`docs/evaluation.md` との食い違いが9件あった
  （同じ陽性窓を検出と誤検出の両方に数える、重なった嚥下の区間を二重に引く、記録の端を切り詰めない、
  区間をまたぐ連続を2回と数える、`--eval-min 0` で全セッションが評価側になる、など）。実装のほうを直し、
  合成データのテスト 42 件で数え方を固定した（`python -m unittest discover -s analysis -v`）。
  定義が決めていない点の解釈（窓の中心で判定、連続は塊で1回、記録の範囲、回数の合算、イベント単位の混同行列）は
  docs/decisions/0011。`docs/evaluation.md` は変えていない。plan レビューは2回（初回 BLOCKING 2、再レビューで3面 LGTM）。
  コミット前の adversarial-review の指摘3件のうち、外れた `t_ms` による記録の長さの水増しと、シンボリックリンクの
  セッションは直した。`aggregate` が呼び出し側を信用する点は CONCERN とし、#13 で経路を1本にする。
  既知の弱点: 常に陽性を出す分類器でも M1 の合格線を通りうる。定義は動かさず、#13 の報告に常時陽性の場合の数字と
  陽性窓の割合を並べ、#14 で人が見る。
- #11: `analysis/features.py` を足した。窓（1.0 秒 / 0.25 秒）ごとに 29 次元（IMU 14: 振幅 4・RMS 6・ピーク数 1・
  主軸方向 3、音 15: MFCC 13・スペクトル重心 1・ゼロ交差率 1）。`docs/evaluation.md` は特徴量の名前だけを決めていて
  式が無いので、実データを見る前に式を docs/decisions/0012 に固定した。#13 の結果を見てから式を動かさない。
  IMU のキャリブレーションは前提にせず、加速度とジャイロの両方で窓内の平均を引く。依存は numpy だけ。特徴量は
  ファイルに保存しない。正規化（`Standardizer`）は渡されたセッションだけから統計量を作る。取りこぼしに掛かる窓は
  落とさず `valid = False` で返す（評価での扱いは #13 で決める）。合成セッションのテスト 34 件（全体で 76 件）が通る。
  60 秒のセッションで約 0.16 秒。plan レビューは3回（BLOCKING 0、CONCERN 7 → 3 → 0）。
  この定義は M1 の PC 側の評価用で、M2 の装置上の実装は保証しない。
- #9: `data/sample/20260920-000000_self_quiet/` に `self` / `quiet` の見本を置いた。音声 10.000 秒（2500 チャンク）と同じ時刻の
  範囲の IMU 1055 行。`sample_index` は 0 始まりに振り直し、`t_ms` は元のまま。`tools/check_session.py` は飛び 0、
  `analysis/features.py` は 36 窓 × 29 次元（#11 の見本での確認）。最初の見本はテレビの音が入っていたので捨て、
  録り直してから人が音声を聞いて確かめた。コミット前の adversarial-review は2回（1回目の指摘2件は CONCERN として
  `data/README.md` に明記、2回目は approve）。見本の `meta.json` には装置の申告値（`fw` など）が無い。基板をリセットせずに
  記録を始めると META フレームが届かないためで、#12 の記録でも起こりうる（扱いは docs/decisions/0006）。
- #15: 音声チャンクの `t_ms` の意味を、文書を実装に合わせる形でそろえた（docs/decisions/0013）。`t_ms` は装置が
  チャンクを読み出した直後の `millis()` で、論理上は終端側の時刻。先頭サンプルの時刻は `t_ms − 1000 × サンプル数 ÷ audio_hz` ms。
  ファームウェアと記録済みのデータは変えていない（読み方だけが変わる）。`analysis/features.py` の換算と音声の飛びの判定を直し、
  テストは 81 件。叩きの実測（50 対）は、どちらの読み方でも ±10 ms 程度（中央値 −3.9 / −7.9 ms）で、4 ms を切り分ける
  分解能は無い。送信中のバッファの上書きの競合は塞がっていない点として 0013 に記録した。plan レビューは3回
  （BLOCKING 0、CONCERN 5 → 1 → 0）。
- #13（スクリプトまで）: 実データを見る前に、学習と評価のやり方を `analysis/train_eval.py` とテストで固定した
  （docs/decisions/0014）。学習のラベルは窓の中心が `[t, t + 1.0]` なら陽性・境目は使わない、モデルは
  `LogisticRegression(C=1.0, class_weight="balanced")` で探索なし、閾値は学習側の交差検証（収集日ごと）で誤検出 3.0 回/分以下の
  うち検出率が最大、正規化は学習側の `valid` な窓の全部。既定の実行は学習側だけで、`--final` のときだけ評価側を採点する。
  報告には、学習と評価のセッションの一覧、混同行列、常時陽性の場合の数字、陽性窓の割合を出す。`scikit-learn` を足した。
  テストは全体で 120 件。plan レビューは2回（BLOCKING 0、CONCERN 4 → 0）。コミット前の adversarial-review の指摘2件のうち、
  未コミットの変更の検出（`-dirty` の表示、`--final` は止まる）は直し、セッションの中のファイルのリンクやコピーは CONCERN として
  0014 に記録した。**実データでの実行はまだ**（#12 のデータがそろってから、0014 の手順で行う）。
- #16: `tools/record.py` が終わった後、端末に打った文字が表示されなくなるのを直した（docs/log/2026-09-20.md）。
  原因は、マーカー入力の daemon スレッドが `tty.setcbreak()` を戻す `finally` を、プロセス終了時に実行しないこと。
  端末の設定の保存と復元をメイン（`atexit` と `finally`）に移し、入力スレッドは `select` + `os.read` で
  終了の印を見て自分で止まる形にした。`o` の note は元の設定に戻してから読む（打っている文字が見える。打ち終える前に
  終了したら、その `o` は記録しない）。SIGTERM も後始末を通すようにし、`Session.mark()` は `close()` の後は書かない。
  記録の形式・マーカーの時刻の取り方・引数・出力先は変えていない。pty の上で子プロセスを動かすテスト 13 件を足した
  （`tools/test_record_tty.py`。修正前のコードでは 12 件が落ちる）。実機での確認（10 秒の記録。終了後の入力、マーカー、
  `o` の note の表示）は人が行い、問題は無かった。
- 進め方: plan の素案づくりと Codex の plan レビューの実施も subagent に移した（docs/decisions/0016、docs/workflow.md
  「メインと subagent」、codex-review skill）。メインが plan mode にいるのは、確定した plan を承認する間だけ。
  `plan-approve.sh` はメインだけが実行する（plan を書いた本人が承認しない）。ゲートの hook と
  「Issue の着手は plan mode から」は変えていない。
- #12（着手）: 人が試しに1セッション（180 秒）を録った。記録は取りこぼし 0.000%（IMU・音声とも飛び 0）、マーカーの
  キーは全部記録された。試しの1本は1セッションに全部の条件を入れていたので、本番には使わず消す。手順の読み違い
  （1セッションで全部の条件を行う）が元だったので、`docs/recording-protocol.md` の冒頭に早見表（7本のコマンド、
  打つマーカー、回数の目安）を足し、「1セッションで行う条件は1つだけ」を明記した。1セッション 180 秒、回数、基準は
  変えていない（300 秒にする案は、読み違いが分かったので取り下げた）。
- #12（収集日1）: 7セッションを録り、人が収集日1として採用した（docs/log/2026-09-21.md に集計表）。`s` 32、`t`+`c`+`n` 45
  （20 / 10 / 15）、`quiet` 3 分。取りこぼしは全部基準内。採用を決めた後で、解析の経路と信号を集計値で確かめた
  （`features.py` は7本とも通る。`s` の 32 回すべてでマーカーの位置に IMU と音の信号がある。首の動き・咳・発話は
  嚥下より大きく出るので、大きさだけでは区別できない。区別できるかは未確認）。`train_eval.py` は収集日が3日そろうまで
  設計どおり止まる。人の決定で、収集日1の7本だけの交差検証（セッションごとの fold、既存の `cross_validate` をそのまま呼ぶ使い捨て）を
  1回だけ走らせた: 閾値 0.95 で検出 22/32、誤検出 60 回 / 19.94 分 = 3.01 回/分（混同行列とセッション一覧は log）。
  同じ日・同じ装着の中の数字で楽観側に出るので、合格線との比較にも #13 の報告にも使わない。誤検出の 65% は `neck` と
  `cough` のセッション。式（0012）と手順（0014）は動かしていない。「各50回以上」にはまだ届いていない。
- #12（収集日2）: 7セッションを録った（docs/log/2026-09-22.md に集計表）。`s` 43、`t`+`c`+`n` 62（22 / 17 / 23）、
  `quiet` 3 分。取りこぼしは全部基準内（IMU 0、音声は最大 0.004%）、`t_ms` の逆行 0、`o` 0。`water` の `s` の間隔
  10 秒未満が4箇所（区間の重なりは無し）、`cough` の中に `s` 2回、2本目の `water` の最後の `s` が記録の端の 0.6 秒前。
  **採否は未決（人が決める）。** 特徴量と分類器は走らせていない。採用すれば収集日1+2で `s` 75、`t`+`c`+`n` 107、
  `quiet` 6 分となり「各50回以上」は満たすが、評価には収集日3が要る（評価側は最後の2収集日）。→ 9/24 に採用。
- #12（収集日3）: 7セッションを録った（docs/log/2026-09-23.md に集計表）。`s` 35、`t`+`c`+`n` 52（22 / 11 / 19）、
  `quiet` 3 分。取りこぼしは全部基準内（IMU 最大 0.005%、音声は最大 0.004%）、`t_ms` の逆行 0、`o` 0。`water` の `s` の
  間隔 10 秒未満は 0、`talk` の中に `s` 1回。2本目の `water` の後に記録ファームウェアがフレームを送らなくなり（USB の列挙は
  残る、約 22 分動いた後）、人が挿し直して回復した。1回きりとして Issue は立てず、収集日4で様子を見る。止まっていた間の
  `20260923-223501_self_cough` は空（0 行、マーカー `c` 2つが `t_ms` 0）で、`cough` は `223530` を使う。
  **収集日2・3の採否と空の `223501` の扱いは未決（人が決める）。** 特徴量と分類器は走らせていない。収集日2・3を採用すれば
  収集日1+2+3で `s` 110、`t`+`c`+`n` 159、`quiet` 9 分となり、分割の決まりの上では評価を始められる（評価側は収集日2+3）。
  人は 9/24 を最終の収集日にする予定で、その場合は評価側が収集日3+4になるので、#13 は収集日4の採否の後に回す。→ 9/24 に採用（空の `223501` は人が消した）。
- #12（収集日4）: 7セッションを録った（docs/log/2026-09-24.md に集計表）。人の決定で収集の最終日。`s` 34、`t`+`c`+`n` 54
  （24 / 12 / 18）、`quiet` 3 分。取りこぼしは全部基準内（IMU 最大 0.011%、音声は最大 0.007%、どちらも `quiet`）、`t_ms` の逆行 0、
  `o` 0。`water` の `s` の間隔 10 秒未満は 0、非嚥下の条件の中の `s` は 0、終わりの 1.5 秒以内の `s` は 0。記録ファームウェアは
  止まらなかった（基板は 7 本を通してリセットなしで約 26.4 分動き、収集日3で止まった約 22 分を越えた）。収集日3の空の
  `20260923-223501_self_cough` は人が `data/raw/` から消していた。**人が収集日1〜4の全部を採用した（9/24）。** 特徴量と分類器は
  走らせていない。累計 `s` 144、`t`+`c`+`n` 213、`quiet` 12 分で、学習側は収集日1+2（`s` 75）、評価側は
  収集日3+4（14本、`s` 69）で確定。`analysis/split.py data/raw --eval-min 14` の `eval` は収集日3+4の14本の名前とちょうど一致した。
  収集日3の挿し直しのとき基板は首から外していない（人に確認）。#12 は人が閉じた（9/24）。次は #13 の実データでの実行。
- #13（実データ）: docs/decisions/0014 の手順どおりに走らせた（docs/log/2026-09-24.md に `--final` の出力の全文）。コードと手順は
  変えていない。コミット `d5330ef`、`--eval-min 14`、学習側は収集日1+2、評価側は収集日3+4（一覧を照合済み）。閾値 0.90（学習側の
  交差検証で決定。交差検証は検出率 0.453、誤検出率 2.61 回/分）。評価側: 検出率 0.783（54/69）、誤検出率 1.99 回/分（79 回 / 39.70 分）、
  混同行列 TP 54 / FN 15 / FP 79。合格線は両方とも満たす。ただし常時陽性でも検出率 1.000、誤検出率 0.35 回/分で合格線を通る
  （docs/decisions/0011「既知の弱点」）。モデルの陽性窓の割合は 0.031。交差検証と評価側の検出率の開きは調べていない。
  誤検出の多いセッションは評価側の `cough`（収集日4、15 回）と `neck`（収集日3、14 回）。`--final` は1回だけで、評価側はこれ以降動かさない。
- #14: 関門のレビュー（Codex、`--base gate-M0`）を1回回した。指摘は1件（セッションのフォルダの中のファイルのリンクやコピーで、
  評価側の中身を学習側に重ねられる）。docs/decisions/0014「塞がっていない経路」と同じ経路で、実データでは起きていない（28 セッションで
  シンボリックリンク 0、リンク数 2 以上のファイル 0、4 種のファイルの sha256 の重複 0）。人の判断で CONCERN。公開の線引きへの指摘は無し。
  判定の材料として事後の参考解析をした（#13 の数字・定義・手順は変えない。集計値は docs/log/2026-09-24.md）。常時陽性が合格線を通るのは
  指標の本当の穴だが、モデルは陽性窓 3.1% で、偶然の基準は 200 シードのどれも検出率 0.783 に届かない。評価側の PR-AUC 0.357（偶然 0.028）。
  **人が M1 を通過とした（9/24、docs/decisions/0017）。** 条件として M2 の課題を3つ明記した: 日をまたいでも崩れないこと
  （交差検証の fold で検出率 0.938 と 0.093）、咳のときの誤検出を減らすこと（咳のマーカーの 48% に誤検出）、IMU と音の結合の方式を
  決めること（評価側の検出率は IMU のみ 0.464、音のみ 0.435、両方 0.783）。今の特徴量とモデルでは、M2 の合格線（85% / 1 回/分）を
  同時に満たす閾値は無い（事後の参考）。人が `gate-M1` タグを `b70a0cb` に打ち、#14 を閉じた（9/24）。
- #19: 一口ボタンは M2 で使わず、LED は緑・黄の2色を「目安」として点ける規則を docs/decisions/0018 に記録した。
  緑は最初の陽性窓で点け、最後の陽性窓から 2.0 秒（開始が 1.75 秒以内の窓まで）保ってから黄に戻る。赤・点滅・音は無し。
  窓の結果が 1.0 秒出なければ消灯（`millis()` の独立したタイムアウト）。
  装置の LED は基板の RGB（WiFiNINA 経由、#25 で `deps` に足す）で、隠れる・処理が収まらない・ビルドできないときは外付け 2 灯に切り替える。
  DETECT の `led` は 0 消灯 / 1 黄 / 2 緑（#23）。点灯の条件を案A（最初の陽性窓）のままにするかは #20 の塊の長さ別の集計で確かめ、
  案C（2 窓連続）に変えるなら #25 の plan で記録に追記する。装置に貼る表示とカードの文面は記録にある。`docs/evaluation.md` は変えていない。
- #20: **参考のみ。#13 の数字は変えていない。** 既存の 28 本で、収集日を 1 日ずつ検証側にする 4 fold（残り 3 日で 0014 の手順）を回した
  （使い捨てスクリプト、集計値は docs/log/2026-09-24.md「#20」）。方式 A の検出率は 0.349（収集日2）〜0.938（収集日1）、誤検出率 1.01〜4.41 回/分、
  ROC-AUC 0.851〜0.947。#21 と同じ分け方（学習側 収集日1〜3、検証側 収集日4）は 0.735 / 1.01 回/分で、M2 の合格線には届かない。
  仮説: H1（学習日数）は 1 日 → 2 日で残り 2 日 → 3 日は判断できない、H2（収集日2 が当てにくい。IMU の主軸方向のずれが最大）は残る、
  H3（確率の目盛りのずれ）は残る、H4（夜の音のずれ）は判断できない。日をまたいで動くのは音の特徴量（ずれの平均 0.205 vs IMU 0.090）。
  日ごとの正規化（検証日の特徴量で正規化した原因切り分けで、運用可能な性能値ではない）で陽性窓の割合の開きは縮まるが、検出率と誤検出率が
  同時に良くなる fold は無い。咳: `c` に紐づく誤検出は全誤検出の 20.8%（37/178）、`c` 50 回の 52% に誤検出が付く。`c` と `s` は 29 次元で
  線形に分かれる（fold の外の ROC-AUC 合算 0.902）ので 3 クラスにする意味はあるが、減る誤検出は最大でも約 2 割。咳の直後の陽性は咳の開始から
  90 パーセンタイルで 2.37 秒続く。`s` の最初の陽性の遅れは中央値 0.35 秒、1 窓の塊の 78% が誤検出（#19 の材料。決めるのは #19）。
- #17: 装置上の特徴量の計算コストを 3 案で測った（`firmware/bench/`、合成信号のみ、docs/log/2026-09-24.md「#17」）。
  1 ホップ（250 ms）あたり: 案3（0012 の IMU 14 次元）8.1 ms で収まる。案2（0012 の音 15 次元の移植）は全窓 4.18 秒、フレーム再利用でも
  1.10 秒。案1（EI の DSP ブロック、ダミー impulse `bench-dummy-17`）は MFCC 全窓 3.55 秒 / スライス 0.73 秒、MFE 2.21 秒 / 0.56 秒。
  音の 3 案はどれも 250 ms に収まらない（FPU なしの soft-float FFT が支配的。案2 は FFT が 1 ホップの約 82%）。RAM は全案で収まる
  （静的＋ヒープ最大＋スタック 32 KB: 案1 155648 B、案2 173660 B、案3 117004 B / 256 KB）。案1 のフラッシュ 241613 B には落ちない tflite の
  ランタイムとモデル 85 KB を含む。見立て: 窓長・次元・周波数のどれか 1 つでは足りず、効くのは 1 ホップのフレーム数（フレーム stride）と
  FFT の回数（8 kHz ＋ 重心なし ＋ stride 20 ms で約 150 ms の見込み。実測ではない）。#18 の判断材料（0012 の式は変えていない。
  docs/decisions/0001 の再検討条件に当たるかは人が決める）。案2 の移植は `analysis/features.py` と答え合わせ済み（最大相対誤差 1e-6）。
  ボードには bench（案1 の裏取りビルド）が入ったまま。logger は書き戻していない。
- #26: M2 の通し記録（`meal`）の手順を `docs/recording-protocol.md` に足した（早見表の小表と末尾の節）。1 セッション = 1 食
  （`--duration 1800` を上限に Ctrl-C で止める。30 分で自動終了した記録もそのまま 1 セッション）、3 セッション以上・合計 `s` 50 以上・会話なし 1・会話あり 1・収集日 2 日以上が
  望ましい。打つのは `s`（嚥下ごと）・`c`（咳ごと）・`t`（話し始め、5 秒以上黙った後に 1 回）で、`b`・`n`・`q` は打たない
  （docs/decisions/0018）。50 口は `s` の合計で数える。非嚥下の回数の下限は置かない。採否の材料は M1 と同じに `fw` が
  `detector` であることを足した。会話ありは記録の最初に `o` の note `conv` で示す。M2 の数字は検出器で録った `meal` だけで出す。
  `record.py` の引数・`detect.csv`・`feat.csv`・`check_session.py` の対応は #23 で確定（手順に「#23 で確定」と明記）。
  68 行目の一口ボタンの記述は 0018 への参照に直した。`docs/evaluation.md`・`docs/data-schema.md` は変えていない。
  plan レビューは 4 回（BLOCKING 1 → 0、CONCERN 4 → 4 → 1 → 0）。
- #18: IMU と音の結合の方式を docs/decisions/0019 に記録した。装置が窓ごとに IMU 14 次元（0012 のまま）と M2 用に削った音の特徴量を自前で計算し、
  1 本のベクトルにして Edge Impulse で学習した分類器（int8）に入れる。EI は分類器の学習・量子化・C++ ライブラリの書き出しだけに使う（EI の DSP は使わない）。
  音の式は 0012 のままでは載らない（#17: 1099 ms）ので、8 kHz・重心は同じ FFT から・stride 20 ms を既定の候補として（`docs/evaluation.md` 8 行目の特徴量の名前の
  並びは保つ。重心を落とす段は評価の定義の逸脱として記録が要る）、#21 の plan で実機の計測と収集日1〜3 の中の確認を通してから新しい記録に固定する
  （0012 は M1 の定義として残す）。投入は `self` 28 セッションの特徴量ベクトル（1 窓 1 項目、正規化の統計量は fit に使うセッションだけ）。ラベルは 0014 のまま。
  学習は 3 クラス（`swallow` / `cough` / `other`）、装置の出力は 2 値で `cough` は陰性に畳む。EI の training は収集日1〜3、test は収集日4、validation はメタデータで
  セッション単位。公開範囲は Public（#21 で投入後に `self` のみ・線引き（所有者の表示名を含む）の確認を log に書いてから切り替え）。C++ ライブラリは書き出しのまま
  コミット（`model_metadata.h` の ID・所有者名は残す。API キーは含まれない）。`docs/evaluation.md`・0012・0014 は変えていない。
- #23: 検出器の DETECT（`0x04`: 窓の開始 `window_t_ms`、`swallow` の確率 float32、`positive`、`led`）と FEAT（`0x05`: `window_t_ms` ＋ 正規化前の特徴量 float32 × N。
  N はフレームの長さ、名前は `meta.json` の `feature_names`）の形式を `docs/data-schema.md` と docs/decisions/0021 に固定した。`frame.h` に enum を足しただけで
  logger は変えていない。`tools/record.py` は `detect.csv`・`feat.csv` を書き、ストリームのファイルは最初のフレームで作る（検出器のセッションに空の `imu.csv` を
  作らない）。マーカーの `t_ms` は送信時刻のまま。`tools/check_session.py` は `window_t_ms` の飛びで取りこぼしを出す（陽性は数えない）。
  `analysis/evaluate_detector.py`（新規）は `detect.csv` の `positive` と `s` から 0011 の数え方で数字を出し、記録の範囲は最初の窓の開始〜最後の窓の終端か
  最後に届いたフレーム（DETECT / FEAT）の送信時刻の遅いほう。送信の遅れは 1.0 秒まで（0011 の読み替え。0021）。`--scorer` で `feat.csv` の再採点と装置の出力の差を出す（scorer の中身は #22）。`analysis/split.py` は
  `fw` が `detector` のセッションを M1 の分割に入れない。テストは合成データで 201 件（`analysis` 172 件、`tools` 29 件）。実機には触っていない。
  plan レビューは 3 回（BLOCKING 2 → 1 → 0、CONCERN 2 → 2 → 0）。
- #21: M2 の音の式を docs/decisions/0020 に固定し（8 kHz・25 ms フレーム・stride 25 ms。`analysis/features_m2.py`、`FEATURE_SET = "m2-0020"`、実機 1 ホップ IMU 込み 134.3 ms）、
  `analysis/ei_upload.py` で正規化後の 29 次元の特徴量ベクトルを 1 窓 1 項目で Edge Impulse に投入した（training = 収集日1〜3 の 21 本 14440 項目、swallow 437 /
  cough 152 / other 13851。testing = 収集日4 の 7 本 4824 項目。生の音声・IMU は上げていない。定数は `analysis/m2_norm.json`、21 本の `valid` な窓 15030）。
  EI のメタデータの鍵による分割は群を指定できず C-1（validation = 収集日3）は成立しなかったので、人の決定で C-2（鍵 `session`、33%。0019 に追記）。EI が選んだ
  validation のセッション名は取得できていない（`cough` の 3 本が含まれないことだけ分かる）。学習は Raw data・3 クラス・Auto-weight・cycles 50・lr 0.0005・batch 32 で、
  候補 B（Dense 32 → Dropout 0.1 → Dense 16）を人が選び（validation int8: accuracy 91.2%、`swallow` F1 0.46、再現率 65.6%。再学習の差 0）、版 1「#21 candidate B 2026-09-24」に
  固定した。test set（収集日4）の Model testing は最後の 1 回: accuracy 85.74%（float32）/ 85.28%（int8）、`swallow` の再現率 52.2 / 54.4%（**窓単位の参考値。合格線は
  イベント単位で #27**）。以後モデル・設定は変えない。**Public に切り替え済み（9/24）**（条件 1 は log に記録。条件 2（プロジェクト名・説明・所有者の表示名）は
  人が確認した。Private に戻す操作は Dashboard にある。戻していない）。plan レビューは 3 回（BLOCKING 0、CONCERN 2 → 1 → 0）。
- #22: 版 1「#21 candidate B 2026-09-24」の C++ ライブラリ（Quantized int8、EON Compiler、deploy version 2、Studio 1.95.14）を `firmware/detector/src/` に書き出しのまま
  置いた（1383 ファイル、編集しない。ラベル順 cough / other / swallow、`scale_axes` 1、EI 側の正規化なし。Public なので ID・所有者名は書き換えない）。PC 上の実行ファイルは
  `bash firmware/detector/host/build.sh` → `build-host/score_windows`（13 秒、警告 0、CMSIS 不要）。正規化の定数は `firmware/detector/m2_norm.h`（`analysis/m2_norm.json` から生成、
  double で計算して float32 に落とす。selfcheck ビット一致）。検証側（収集日4、7 本）の参考値: 閾値 0.95 で検出率 58.8%（20 / 34）、誤検出率 0.75 回/分（15 回 / 19.87 分）、
  TP 20 / FN 14 / FP 15。**85% と 1.0 回/分を同時に満たす閾値は無い**（0.85 で 85.3% / 1.76 回/分。**参考値。M2 の合格線の数字は #27**）。人の決定で 0.95 を
  `firmware/detector/m2_threshold.h` に凍結（float32 0.949999988。#27 の記録が終わるまで閾値もモデルも変えない）。EI の Model testing（int8、API で 4824 窓）との突き合わせは
  **差あり**（閾値での陽性・陰性の不一致 0、argmax の不一致 3。API の 5 桁の丸めを合わせると完全一致 4648 / 差あり 176 窓、最大 43/256。入力の量子化は原因でなく、推論エンジンの
  丸めの違いと推定。許容幅は決めず、装置との一致は #24 で見る）。`firmware/bench/src/`（#17 のダミー）は人が削除。plan レビューは 3 回（BLOCKING 1 → 1 → 0、CONCERN 3 → 2 → 1、
  残りは E1 の突き合わせで解消）。
- #24: 検出器ファームウェア `firmware/detector/` を作った（`detector.ino` ＋ `sensors` / `audio_capture` / `imu_capture` / `pipeline` / `classify` / `detector_meta` / `detector_frames`。
  Arduino / mbed の API は `detector.ino` と `sensors.cpp` だけ、Edge Impulse SDK は `classify.cpp` だけ。bench・logger のソースはシンボリックリンクで参照。コミット `04a84a9`（段階 1）・
  `f72fbf6`（段階 2〜3）、docs/log/2026-09-26.md）。生の音声は 16 kHz の 2 面（最長 0.5 秒）と 8 kHz のリングにしか無く、送信関数の呼び出しは META・DETECT・FEAT の 3 箇所だけ
  （grep は log）。`CMakeLists.txt` は `SKETCH_NAME=detector` で基底フラグを自動付与（`build-detector`。logger は `build` のまま）。PC のテスト 230 件（`bash firmware/detector/host/test_detector.sh`）。
  実機では最初、4 スライス目の全窓計算 1.2 秒で追いつかず窓が出なかったので、`firmware/bench/audio_features.cpp` の再利用版をスライスごとの分割計算に変えた（**plan の「bench を
  変えない」から外れる**。`check_port.py` はビット一致）。次に IMU の poll スレッド（2 ms・I2C 100 kHz）が 1 ホップを 293 ms にしていたので、切り分け P1〜P6 の後、人の決定で
  本番の既定を I2C 400 kHz・poll 4 ms・`-Os` にした（`-O2` は利得 2% で採らない）。**1 ホップ 208 ms**（音 188・IMU 12・推論 6・送信 1。壁時計 最大 214 ms）で 250 ms に収まり、
  docs/decisions/0001 の再検討には当たらない。RAM は静的 161872 B ＋ ヒープ最大 5644 B ＋ スタック（主 32 KB ＋ IMU 2 KB）≒ 200 KB / 264 KB、フラッシュ 159190 B。
  IMU の基準の周期は「直近 512 行の全差分の平均を公称の 1/1.25〜1.25 倍に収める」に変えた（plan の「飛びでない差分の移動平均」は起動直後に自己固定して約 11 秒窓が出なかった）。
  60 秒の記録 `20260926-114039_self_water`（装着、水 5 回）: DETECT 206 / FEAT 206 / META 1、XOR 0、飛び 0、逆行 0、DETECT と FEAT の不一致 0。PC の再採点（`--scorer`）との差は
  装置 1 / PC 0 = 0、装置 0 / PC 1 = 0、`max |prob − prob_pc|` = 0（ビット一致）。**#24 の完了の条件 4 つを満たした。** 参考値（1 セッション。M2 の数字ではない）: 検出 4 / 5、
  誤検出 0 回。記録は `data/raw/` に残す（M1 の分割にも #27 の既定にも入らない）。マーカーの `t_ms` は押した時刻より 0〜0.25 秒早い側（装着 120 秒、10 回、d1 0.1〜228 ms）で
  `docs/recording-protocol.md` の前提と合う。**人に諮ること: 送信の遅れの上限 1000 ms（0021）を詰めるか。** 実測は `t_ms − window_t_ms` 1201〜1205 ms（窓の終端から 201〜205 ms）。
  詰めるなら 0021 に追記して `analysis/evaluate_detector.py` を直す（別の作業、Codex のレビュー）。確かめていないこと: IMU スレッドのスタック高水位（走査が 2044 を返す）、
  DRDY 割り込み（未実装。要らなかった）、実信号での特徴量の正しさ（bench の合成信号の答え合わせに依る）、窓の数 206 が見込み 236〜238 より少ない理由。
  **実機には本番の detector が入ったまま。logger は書き戻していない。**
- #25: 検出器に LED の表示（嚥下の「目安」。判定ではない）を載せた。`led_rule`（0018 の規則。Arduino 依存なし）と `led_out`（基板の RGB LED を NINA 経由で書く。黄 = 赤＋緑。
  WiFiNINA の API は `led_out.cpp` だけ）、DETECT の `led` は実際に表示している状態、1.0 秒結果が出なければ消灯。PC 側は `analysis/led_rule.py` で再計算し、`evaluate_detector.py` の
  参考値に `led` の不一致の行数を出す。コミット `795773a`、docs/log/2026-09-26.md「#25」。WiFiNINA 2.1.1 / Arduino_SpiNINA 0.0.2。本番 160682 B / 162048 B（#24 比 +1492 / +176 B）。
  実機: 極性 `HIGH` = 点灯で正しい（起動直後は消灯、ポートを開くと黄）。止めると約 1 秒以内に消灯。プロファイル 60 秒（装着）で `total_max` 207.9 ms・`wall_max` 212 ms、
  `digitalWrite` 1 回の最大 373 µs、`led_skipped` 0、飛び 0 で 250 ms に収まった。記録 A `20260926-142926_self_water`（水 6 口）と B `20260926-144044_self_cough`（咳払い 3・首 3）で
  `led` の不一致 0、PC の再採点との差 0、飛び 0。緑の遅れ（A。緑ありは 6 口のうち 3 口）中央値 750 ms・最大 1250 ms、嚥下に紐づかない緑は A 1 回・B 0 回（参考。M2 の数字ではない）。
  **0018 の切り替え条件 (a) に当たった: 基板の LED は喉に当てる面にあり、装着すると見えない。** (b)(c) には当たらない。緑は目で確かめられていない（データでは緑への書き込み 37 行）。
  **人に諮ること: 外付け 2 灯（RP2040 の GPIO 直結の緑・黄）へ切り替えるか（切り替えの実装は別の Issue）、案A のままか案C にするか。** どちらも未決。
  META の無い B の 2 本（USB を挿し直しても META が届かなかった回を含む）は評価に使わない。**実機には LED 付きの本番 detector（`795773a`）が入ったまま。logger は書き戻していない。**
- #29: 検出器の基板の LED が装着で見えない（0018 の (a)）ので、もう 1 枚の Nano RP2040 Connect を表示器にし（`firmware/indicator/`）、`record.py --indicator` が
  DETECT の `led` を 1 バイトずつ USB シリアルで中継する形にした（人の決定。0018 に追記）。検出器のファームウェアと記録の形式は変えていない。表示器の緑 → 黄を目で確かめた（6/6 口）。
  表示器の有無で検出器の記録は変わらない（セッション C `20260926-161925_self_water`・D `20260926-162247_self_water`（途中で表示器を抜いた。記録は続き、送った 58 / 失敗 134）とも
  取りこぼし 0%、飛び 0、`t_ms − window_t_ms` 1202〜1208 ms、`led` 不一致 0、再採点の差 0。#25 の A と同じ）。PC → 表示器の往復（同期で送ったとき。状態が変わる回）
  中央値 0.82 / 最大 2.13 ms（本番の送信スレッドの起床と PC の読み取りの待ち 最大約 50 ms は含まない。測っていない）。コミット `8680504`（段階 1）、indicator 本番 94961 B / 43828 B。
  書き込みは by-id のパスでは 1200 bps のタッチの後にポートを見失って失敗し、実体の `/dev/ttyACM1` で通った（README と `CMakeLists.txt` の書き込みの例は実体のパスに直した）。
  docs/log/2026-09-26.md「#29」。BLE への置き換えは #30。**実機: 検出器に本番 detector（`795773a`）、表示器に本番 indicator（`8680504`）が入っている。logger は書き戻していない。**
- ロボセンサー技研への問い合わせ未送付（代替センサ、docs/decisions/0002、#5）。

## 次にやること

M2 検出器（9/28〜10/11）: Issue #17〜#28 を 9/24 に起票した（草案は人がレビューし、起票後に削除）。合格線は検出率 85% 以上、
誤検出 1 回/分以下。docs/decisions/0017「M2 への課題」の3つ（日をまたいでも崩れない → #20・#21、咳のときの誤検出 → #20・#21・#26、
IMU と音の結合の方式 → #17・#18）に答える。

| Issue | 内容 | 依存 |
|---|---|---|
| #17 | 装置上での特徴量の計算コストを3案で測る（EI の MFCC/MFE、M1 の音の特徴量の移植、IMU 14 次元） | なし |
| #18 | 結合の方式・EI に投入するデータ・公開範囲を決める（3クラスの線引きを含む） | #17 |
| #19 | 一口ボタン（M2 では使わない）と LED（緑・黄のみ）の規則を決める | なし |
| #20 | 日をまたぐ崩れ方と咳の窓の分かれ方を既存データで測る（使い捨て解析） | なし |
| #21 | EI にデータを投入し、impulse を設計して学習・int8（学習側は収集日1〜3、検証側は収集日4） | #18、#20 |
| #22 | C++ ライブラリを書き出し、PC 上で検証側に掛けて閾値を凍結する | #21 |
| #23 | DETECT / FEAT ストリームの形式、`record.py`・評価スクリプトの対応 | #18 |
| #24 | 検出器ファームウェア（生の音声を送らない。閾値は #22 の値を固定） | #22、#23 |
| #25 | LED の表示（「目安」の文言） | #19、#24 |
| #26 | 記録手順に `meal` を足す（実際の食事、会話なし1・会話あり1を含む3セッション以上） | #19 |
| #27 | 検出器で 50 口の通し記録を行い、M2 の数字を出す | #23〜#26 |
| #28 | 【判定】M2 検出器（閉じるのは人、`gate-M2` タグは人） | 全部 |

起票前に人が決めたこと（9/24）: 一口ボタンは M2 では使わない。LED は緑・黄のみ。EI の学習側は収集日1〜3・検証側は収集日4で、
閾値は収集日4で決めて凍結し、全4日で学習し直さない。学習は3クラス（嚥下／咳／その他）でもよいが装置の出力は2値に畳み、咳を「むせ」として
出さない。検出器は窓ごとの特徴量（FEAT ストリーム）を PC に送ってよい（生の波形ではないので docs/decisions/0005 の範囲内。`self` のみ、`p1` は別途）。
C++ ライブラリはコミットする。RP2040 で 250 ms に収まらないときは #28 で決める。
**今（9/26）:** #29 で表示器に切り替えた。#27 の `meal` の記録は始められる（`meal` では表示器をつながない）。#30（BLE）は #27 の後に人が時期を決める。案A のまま。
並行できる組は #17 / #19 / #20、#21 / #23。実装の subagent が動いている間は plan mode に入らない。
次の関門（M2）のレビューは `--base gate-M1` で回す。

マイルストーンなし（M0 は 9/22 にクローズ）

- #5 代替センサ（ロボセンサー技研）の入手性を問い合わせる（人、優先度最低）。

## 保留中の判断

- 代替センサの部品発注（圧電コンタクトマイク、docs/decisions/0002）。人の作業。#28 で届かなかった場合に M3（10/12）まで間に合うよう、#28 の前に発注するかを人が決める。
- `p1` の記録先（リポジトリごと暗号化ディスクに置くか、`p1` だけリポジトリ外を必須にするか）。M3 本人試用（10/12）の前に決める。
