# 0010 M0 準備の関門を通過とする

日付: 2026-09-20　状態: 採用

## 決定

- M0 準備の関門（#6）を通過とする。判定したのは人。
- `gate-M0` タグは `045dd7c`。次の関門（M1、#14）のレビューは `--base gate-M0` で回す。

## 判定基準と確認の結果

判定基準（`docs/plan.md`）: ビルドと書き込みが通る。決定事項がすべてファイルにある。

- #1〜#4 は閉じている。#5（代替センサの問い合わせ）は開いたまま（#6 の条件上、閉じていなくてよい）。
- `cmake --build build --target build` と `--target upload` が通った記録は `docs/log/2026-09-19.md`。
- ファイルになっていない決定は、`docs/status.md` の「保留中の判断」の4件以外に見つからなかった。
- #2 で、plan レビューのゲートが Codex を実際に呼ぶ形で通っている（#2 のコメント）。
- 関門のレビュー（Codex、base は初期コミット）は3回。BLOCKING は残っていない。
  - 指摘1（`--out`）と指摘2（`cond`）は採用して直した。指摘3（`data/raw` のシンボリックリンク）は
    CONCERN とし、運用で注意する（docs/decisions/0007）。
  - 公開してよい情報の線引きへの指摘はなかった。
  - 結果、裏取り、Codex の生の出力、採否は #6 のコメントにある。

## この関門で変えた運用

- plan の再レビューは前回の Codex スレッドを resume する（docs/decisions/0008）。
- 再レビューの回数の上限をなくした（docs/decisions/0009）。
